# rule 2: binds a tool absent from the taxonomy (fail-closed). (from tests/agent-capability/kyverno/bad-agents.yaml)
apiVersion: kagent.dev/v1alpha2
kind: Agent
metadata:
  name: bad-unclassified-tool
  namespace: kagent
  labels:
    capability.homelab/class: admin
spec:
  declarative:
    tools:
    - type: McpServer
      mcpServer:
        name: kagent-tool-server
        toolNames:
        - k8s_get_resources
        - k8s_brand_new_tool_from_chart_bump

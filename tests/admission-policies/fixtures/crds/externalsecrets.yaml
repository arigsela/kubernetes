# Minimal stand-in for the real externalsecrets.external-secrets.io CRD. Field names and types are copied from the
# real schema (external-secrets 2.8.0, v1) for exactly the paths the admission policies read, so a
# misspelled path fails CEL type checking here; everything else is preserved as unknown
# fields. The live cluster's status.typeChecking remains the authority on the full schema.
# When a policy starts reading a new field, add it here from the real CRD.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: externalsecrets.external-secrets.io
spec:
  group: external-secrets.io
  scope: Namespaced
  names:
    kind: ExternalSecret
    listKind: ExternalSecretList
    plural: externalsecrets
    singular: externalsecret
  versions:
  - name: v1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          apiVersion: &id001
            type: string
          kind: *id001
          metadata:
            type: object
          spec:
            type: object
            x-kubernetes-preserve-unknown-fields: true
            properties:
              secretStoreRef: &id002
                type: object
                x-kubernetes-preserve-unknown-fields: true
                properties:
                  name: *id001
                  kind: *id001
              refreshInterval: *id001
              target:
                type: object
                x-kubernetes-preserve-unknown-fields: true
                properties:
                  name: *id001
              data:
                type: array
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                  properties:
                    secretKey: *id001
                    remoteRef:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                      properties:
                        key: *id001
                        property: *id001
                    sourceRef:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                      properties:
                        storeRef: *id002
              dataFrom:
                type: array
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                  properties:
                    extract:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                      properties:
                        key: *id001
                        property: *id001
                    sourceRef:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                      properties:
                        storeRef: *id002
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true

# McpServer tool refs that list their tools explicitly.
apiVersion: kagent.dev/v1alpha2
kind: Agent
metadata:
  name: good-agent
  namespace: kagent
  labels:
    # Good fixtures must pass EVERY policy, not just the one their suite is named after.
    capability.homelab/class: read
spec:
  type: Declarative
  declarative:
    tools:
      - type: McpServer
        mcpServer:
          apiGroup: kagent.dev
          kind: RemoteMCPServer
          name: kagent-tool-server
          toolNames: [k8s_get_resources]
      - type: Agent
        agent:
          name: k8s-reader

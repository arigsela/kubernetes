# SPEC

## §G GOAL
**REACHED 2026-07-29** — ∀ 3 node @ `v1.35.6+k3s1`, ⊥ data loss. walk 1.33.5 → 1.34.9 → 1.35.6 complete (§T.17, §T.18, §T.19). remaining ∈ this spec = follow-ons + §T.42/§T.45 drift + §T.46/§T.47.

k3s cluster 1.33.5 → **`v1.35.6+k3s1`** (§R.18: 4 components cap @ 1.35, ingress-nginx TERMINAL ∴ 1.36 ⊥ reachable). 1.36 = FOLLOW-ON, gated on replacing ingress-nginx (§T.43). platform components @ matrix ceiling per minor, INTERLEAVED w/ walk where matrix demands (§V.13); ESO DEFERRED past the walk (§V.48); ⊥ data loss.

## §C CONSTRAINTS
- GitOps only. ∀ change → git commit → Argo CD sync. ⊥ direct `kubectl apply` (CLAUDE.md).
- cluster = 3 node k3s. `k3s-control-01` server, `k3s-worker-01` + `k3s-worker-02` agents. Ubuntu 24.04.3, containerd 2.1.4-k3s1.
- server args = `server --disable traefik --write-kubeconfig-mode 644`. ⊥ `--cluster-init` ∴ SQLite datastore, ⊥ embedded etcd.
- ⊥ `k3s etcd-snapshot` (SQLite ∉ etcd). backup = fs copy `/var/lib/rancher/k3s/`.
- k3s minor upgrade migrates DB one-way ∴ downgrade ⊥ supported. rollback = fs restore + reinstall @ prior `INSTALL_K3S_VERSION`.
- 1 server ∴ ∀ control-plane hop → outage of API + Vault + CNPG. accepted, bounded @ §V.9, §V.14.
- ⊥ HA conversion ∈ this spec. 3-server embedded etcd = follow-on §T.24.
- k8s skew ! ≤ ±1 minor ∴ walk 1.33 → 1.34 → 1.35 → 1.36. ⊥ skip minor.
- mechanism = system-upgrade-controller. Plan CRs ∈ `base-apps/`.
- 92 workloads ∈ 45 ns.
- CNPG `postgresql/postgresql-cluster` instances=1 ∴ ⊥ replica ∴ restart = data-path outage.
- `local-path` = only SC & default, reclaim=`Delete`, 21 PVC ∀ on it. PV `nodeAffinity` hard-pins pod → node ∴ ⊥ reschedule on drain.
- `vault-0` + `postgresql-cluster-1` ∈ `k3s-control-01`, PV pinned there ∴ control-plane hop = secrets + DB outage, ⊥ evacuable w/o PV migration.
- ∀ Argo app `prune: true` + `selfHeal: true` ∴ prune of PVC → PV Delete → data gone.
- `kube-system/ingress-nginx` = HelmChart CR (`base-apps/nginx-ingress/`) reconciled by k3s embedded helm-controller ∴ helm-controller upgraded by ∀ hop.
- Vault in-cluster ∴ ESO break → ∀ app secret resolution stops. ESO ! healthy before ∀ hop.
- Vault seal = `awskms` (`alias/vault-auto-unseal`, `terraform/roots/asela-cluster/vault-kms.tf`). `/vault/data` bytes = CIPHERTEXT ∴ ∀ fs backup undecryptable w/o KMS key. Shamir recovery keys govern recovery ops, ⊥ storage decryption.
- Istio = ambient (istiod + `ztunnel` DS + `istio-cni-node` DS) ∴ node-level dataplane, hop risk ≥ sidecar.
- k8s 1.33 EOL 2026-06-28 ∴ cluster currently unsupported.

## §I INTERFACES
- file: `base-apps/system-upgrade-controller.yaml` → Argo CD Application
- file: `base-apps/system-upgrade-controller/*.yaml` → SUC deploy + RBAC + CRD
- file: `base-apps/system-upgrade-controller/plan-agent.yaml` → Plan CR, workers ONLY
- file: ⊥ `plan-server.yaml`. control-plane hop = manual (§V.17) ∴ SUC ⊥ ever target `k3s-control-01`
- cmd: `scripts/vault-backup.sh --dest <dir\|s3://> [--mode cold\|online] [--argo-app NAME]` → `vault-0` `file` storage → sha256 → off-cluster. cold = suspend Argo app (§V.33) → scale sts 0 → helper pod w/ control-plane toleration (§V.34) → scale back → ! verify unseal. online ! `--allow-inconsistent` & brands artifact `INCONSISTENT`
- cmd: `scripts/vault-restore.sh --artifact <a> --data-dir <d> [--verify-cmd C] [--accept-inconsistent]` → checksum gate → refuse `INCONSISTENT` w/o ack → restore → ! prove unseal + secret read
- node-label: `k3s-upgrade=true` → gates Plan `nodeSelector`
- cmd: `scripts/k3s-backup.sh --mode cold\|online --dest <dir\|s3://>` → archives `server/` ONLY (⊥ `storage/` = local-path PV data, ⊥ `agent/`, ⊥ `kine.sock`) → sha256 → off-cluster. cold ! k3s stopped; online = `sqlite3 .backup` + excludes live db file set
- cmd: `scripts/k3s-restore.sh --artifact <a> --expect-version <v> [--skip-service] [--verify-cmd C]` → checksum gate → prior tree moved aside (⊥ destroy evidence) → promote snapshot → reinstall pinned `INSTALL_K3S_VERSION` → ! prove API @ expected version
- cmd: `scripts/pg-backup.sh --dest <dir\|s3://> --all \| --database <db>` → `pg_dumpall` (globals+roles) \| `pg_dump` via CNPG primary → sha256 → off-cluster. ⊥ default db name (cluster hosts `n8n`+`chores_tracker`). operator-independent ∴ survives §T.9
- backup: CNPG barman → `s3://mysql-backups-asela-cluster/postgresql/`, `ScheduledBackup postgresql-daily-backup` 02:00 UTC, 30d retention. healthy & archiving, restore ⊥ proven (§T.34)
- store: off-cluster artifact target. `s3://mysql-backups-asela-cluster/` ∃ already ∴ add `k3s/` + `pg/` prefixes. ⊥ new bucket needed
- test: `tests/k3s-upgrade/` = pytest oracle ∀ §V.1,6,10,15,16,21,28. drills boot real k3s + postgres + vault in docker. CI job `k3s-upgrade-scripts` ∈ `.github/workflows/validate.yaml`
- doc: `docs/plans/k3s-1.36-upgrade-plan.md` → runbook + outage comms
- doc: `docs/plans/k3s-1.36-api-scan.md` → §T.4 report. built-in APIs clean 1.34/1.35/1.36; pluto ⊥ see CRD removals ∴ ESO `v1beta1` = real gap
- doc: `docs/plans/ingress-nginx-replacement-handover.md` → §T.43 verified surface (live truth 2026-07-29): traffic path, 25 Ingress / 21 host / 30 file, annotation inventory, options A-E

## §R RESEARCH
id|topic|finding|src
R1|ESO v1beta1 removal|`v0.17.0` stops serving `v1beta1`. ! migrate manifests → `v1` BEFORE `0.16`→`0.17`. change = drop `beta1` only|github.com/external-secrets/external-secrets/releases/tag/v0.17.0
R2|ESO last v1beta1 build|`v0.16.2` = last serving `v1beta1` — chartmuseum chart ONLY, ⊥ OCI variant. repo @ `https://charts.external-secrets.io` ∴ correct source already|github.com/external-secrets/external-secrets/issues/5478
R3|CRD storage version|live `externalsecrets`+`secretstores`+`clustersecretstores` ∀ `status.storedVersions=["v1beta1"]`, conversion=`Webhook`. k8s ⊥ drop version ∈ storedVersions ∴ ! rewrite ∀ stored object as `v1` + prune storedVersions before `0.17.0`. git ≠ etcd|kubectl, local truth 2026-07-27
R4|ESO ↔ k8s matrix|`≤0.13`:1.19-1.31 / `0.15-0.18`:1.32-1.33 / `0.19`:1.33 / `0.20`:1.34 / `1.0-1.3`:1.34 / `2.0-2.6`:1.34-1.35 / `2.7`:1.35. ⊥ version supports 1.36|external-secrets.io/latest/introduction/stability-support/
R5|ESO already out-of-matrix|`0.11.x` unsupported upstream, matrix = k8s ≤1.31, cluster @ 1.33 ∴ out-of-matrix now — same posture as Istio `1.24.0`|external-secrets.io/latest/introduction/stability-support/
R6|ESO latest|`v2.8.0`. k8s 1.36 support ⊥ stated in release notes ? — ! confirm before §T.21|github.com/external-secrets/external-secrets/releases
R7|Argo drift #5478|`0.16.x` webhook rewrites stored objects `v1beta1`→`v1` → drift vs git. CLOSED 2025-11-23, resolution = migrate manifests, ⊥ a code fix ∴ pause = migration window only|github.com/external-secrets/external-secrets/issues/5478
R8|CNPG rollout triggers|doc lists `imageName`, image-catalog, pg config needing restart, `spec.resources`, PVC resize (AKS), operator update. affinity ⊥ listed — BUT EMPIRICALLY DOES restart the primary: "Primary instance is being restarted without a switchover" observed on affinity patch 2026-07-28 (§B.5). doc trigger list ⊥ exhaustive ∴ absence of mention ≠ absence of behaviour|cloudnative-pg.io/documentation/1.24/rolling_update/ + observed, local truth 2026-07-28
R9|CNPG relocate procedure|documented: cordon host node → `instances` 1→2 → operator AUTO-switchover → scale→1 (drops original) → drain old node. NB procedure NEVER changes affinity — the CORDON is what redirects the new instance|cloudnative-pg.io/documentation/1.24/kubernetes_upgrade/
R10|CNPG PDB blocks drain|`enablePDB=true` live; PDB `postgresql-cluster-primary` ALLOWED DISRUPTIONS=0 ∴ drain of `k3s-control-01` BLOCKED while single instance there. gates §T.17|kubectl, local truth 2026-07-28
R11|CNPG live affinity|`nodeSelector: workload=infrastructure` + control-plane toleration + `podAntiAffinityType: preferred` ∴ 2 instances ? co-locate on 1 node (preferred ⊥ required)|kubectl, local truth 2026-07-28
R12|`kubectl cnpg` absent|plugin ⊥ installed ∴ ⊥ `cnpg promote` manual switchover. `primaryUpdateStrategy=unsupervised` ∴ auto-switchover expected. ! install as §T.37 fallback lever|local truth 2026-07-28
R13|ingress-nginx TERMINAL|`kubernetes/ingress-nginx` repo ARCHIVED (`archived:true`, last push 2026-03-23). final release `controller-v1.15.1` 2026-03-19 = EXACTLY the version in cluster. supports k8s 1.31-1.35 ∴ ⊥ 1.36 EVER. upstream: adopt Gateway API|github.com/kubernetes/ingress-nginx via gh api
R14|Gateway API ∃ already — SUPERSEDED by §R.26|4 GatewayClass live & Accepted: `istio`, `gloo-gateway-v2`, `agentgateway-enterprise`(+waypoint). `istio-waypoint` Gateways serving `chores-tracker`+frontend 217d ∴ migration path ∃, ⊥ greenfield. **WRONG — 2026-07-29 re-verify: those Gateways = ambient WAYPOINTS (east-west), ⊥ ingress; HTTPRoute = 0 ∴ ≈ greenfield. see §R.26**|kubectl, local truth 2026-07-28
R15|Argo CD ⊥ 3.5 GA|`v3.5.0` = rc1/rc2/rc3 ONLY (rc3 2026-07-28). latest STABLE = `v3.4.5` 2026-07-09 ∴ §V.7 satisfiable only by DOWNGRADE → 3.4 line \| wait for 3.5.0 GA|github.com/argoproj/argo-cd/releases
R16|kyverno matrix|`v1.18` = k8s 1.33-1.35 ∴ ⊥ 1.36. cluster @ `v1.17.1`, older still. kyverno = admission webhook ∴ incompat blocks ∀ pod create|kyverno.io/docs/installation/releases/
R17|cert-manager matrix|`v1.20` = k8s 1.32-1.35; `v1.21` = 1.33-1.36 ∴ REACHES 1.36 via upgrade. ⊥ blocker|cert-manager.io/docs/releases/
R18|1.36 CEILING VERDICT|4 components cap @ 1.35: CNPG(§R.4-adjacent), ESO(§R.4), ingress-nginx(§R.13, TERMINAL), kyverno(§R.16). ∴ **1.35 = the real target**. 1.36 ⊥ reachable w/o replacing ingress-nginx entirely|synthesis §R.4,13,16
R19|SSA ⊥ the drift fix|5 of 8 drifting apps ALREADY have `ServerSideApply=true` (istio-base, kagent-secrets, kyverno, openshell, openshell-secrets) & drift regardless ∴ §T.42 premise FALSE. drift causes heterogeneous: istio = `caBundle` (1460B) injected by istiod ∉ git; others = SA, Job, StatefulSet, ExternalSecret, CRD. Argo docs: SSA "has the potential to be destructive and might lead to resources having to be recreated, which could cause an outage"|argo-cd.readthedocs.io/en/stable/user-guide/sync-options/ + kubectl 2026-07-28
R20|1.35 audit ≠ 1.36 audit|retarget → 1.35 REMOVES work. `ingress-nginx v1.15.1` (1.31-1.35) & `cert-manager v1.20.2` (1.32-1.35) ALREADY cover the target ∴ ⊥ upgrade needed. only CNPG + Istio + kyverno remain|synthesis §R.13,17 vs §G target
R21|chart `kubeVersion` ≠ published matrix|kyverno chart `3.7.1`(v1.17.1) declares `kubeVersion: '>=1.25.0-0'` — NO upper bound; crossplane & istio charts declare none. ∴ published matrices = TESTED-version statements, ⊥ install-time blocks. nothing refuses to install @ 1.35|helm show chart, local truth 2026-07-28
R22|CNPG ⊥ skip minors|docs: "upgrading through each version in sequential order... not skipping versions". ∴ §T.9 = 5 STEPS ⊥ 1: chart `0.23.2`→1.25.1, `0.25.0`→1.26.1, `0.26.1`→1.27.1, `0.27.1`→1.28.1, `0.28.3`→1.29.1. operator upgrade → instance rollout; `instances=1` ∴ ⊥ switchover possible, pod RESTARTS each step (~30-60s DB down ×5). `ENABLE_INSTANCE_MANAGER_INPLACE_UPDATES` would avoid it but breaks pod immutability ∴ ⊥ used|cloudnative-pg.io/docs/1.29/installation_upgrade/ + charts index.yaml
R23|Istio ambient upgrade|order = base(CRD) → istiod → cni → ztunnel → gateways/waypoints. istiod = revisioned + tags; `istio-cni` ⊥ canary-able ("Istio does not currently support canary upgrades of istio-cni") ∴ cni + ztunnel = IN-PLACE. "Upgrading ztunnel in-place will briefly disrupt all ambient mesh traffic on the node". CRITICAL: cni+ztunnel `1.x` compatible w/ control plane `1.x`..`1.x+1` ONLY ∴ 1-minor skew MAX ∴ `1.24`→`1.29` = 5 SEQUENTIAL steps × 4 components, ⊥ one jump|istio.io/latest/docs/ambient/upgrade/helm/
R24|Istio k8s matrix|`1.24` tested 1.28-1.31 ∴ ALREADY out of tested range on this cluster & working. `1.25` 1.29-1.32. `1.26`/`1.27` 1.29-1.33. `1.28` 1.30-1.34. `1.29` 1.31-1.35. **`1.30` = k8s 1.32-1.36 — CONFIRMED 2026-07-29 × 2 source ∴ §V.60's `?` RESOLVED, `1.30` REACHES 1.36.** ONLY `1.29`+`1.30` supported upstream & **`1.29` EOL ~2026-08** ∴ `1.30` = the only non-EOL target, ⊥ merely preferred. NB "passing THROUGH `1.25` ⊥ worse than status quo" was computed @ k8s **1.33**; cluster now 1.35 ∴ `1.25`-`1.28` ∀ ∉ matrix → §V.64|istio.io/latest/docs/releases/supported-releases/ + istio.io/latest/news/releases/1.30.x/announcing-1.30/
R25|ingress traffic path|internet → `10.0.1.50` `:80`/`:443` → ingress-nginx (`hostNetwork` DS, 1 replica, pinned `k3s-control-01` via `node.kubernetes.io/workload: infrastructure`) → ClusterIP → pods. ⊥ reverse proxy, ⊥ tunnel, ⊥ WAF. access log = REAL public client IP (`74.248.24.145`, `164.92.144.52`, `205.210.31.169`) + hourly vuln scan ∴ ∀ exposed host found within a day. Cloudflare tunnel removed 2026-07-29 (`9e53db8`)|live cluster + handover §3, 2026-07-29
R26|Gateway API ⊥ serving ingress|CORRECTS §R.14. CRD ∃: `gateways`,`httproutes`,`grpcroutes`,`referencegrants`,`backendtlspolicies` (standard channel; ⊥ `tlsroutes`/`tcproutes`). 7 GatewayClass Accepted. BUT the 2 live Gateway = `istio-waypoint` AMBIENT WAYPOINT (east-west policy ∈ `chores-tracker`,`-frontend`), ⊥ north-south. **HTTPRoute = 0**. Accepted = a controller claimed the class, ≠ serving ∴ migration ≈ GREENFIELD|kubectl, live truth 2026-07-29
R27|cert-manager = nginx consumer|24 Certificate; **20 @ `letsencrypt-prod` → HTTP-01 solved through `ingress.class: nginx`** ∴ nginx removal breaks renewal ∀ 20. solver creates its OWN transient `Ingress` ∴ issuance itself consumes nginx. 3 @ internal `openshell` CA, unaffected. failure DELAYED ~30d @ renewal, ⊥ visible @ cutover|live cluster 2026-07-29 + `base-apps/cert-manager/`
R28|`letsencrypt-route53` = escape hatch|ClusterIssuer ∃ & Ready ALREADY, DNS-01 via Route 53, creds ← Vault `cert-manager/route53` (ExternalSecret `route53-credentials`, 1h refresh). used by exactly 1 cert (`base-apps/vcluster-sandbox-1/certificate.yaml`) ∴ proven in-cluster. NB ⊥ staging issuer on the DNS-01 path ∴ testing hits PROD ACME directly (rate limits)|`base-apps/cert-manager/letsencrypt-route53.yaml` + runbook, repo truth 2026-07-29
R29|migration surface|30 file match `ingress-nginx` \| `ingressClassName: nginx` ∈ `base-apps/`+`terraform/`. repo yaml = 21 `kind: Ingress`; live = 25 Ingress / 21 hostname. delta live>repo **UNEXPLAINED** ? — cert-manager solver Ingress + class-less `atlantis/atlantis` (inert) account for some; the "Rollouts canary/stable copies" guess was WRONG (the 1 Rollout routes via VirtualService, ⊥ Ingress — §R.36) ∴ ! ENUMERATE LIVE before §T.56|grep repo + live cluster 2026-07-29
R30|annotation → ⊥ Gateway API equivalent|`whitelist-source-range` **17 live / 15 ∈ repo yaml (14 file)** — ⊥ equivalent, HARDEST; `ssl-redirect`/`force-ssl-redirect` 21 file — trivial (`RequestRedirect`); `limit-rps`+`limit-connections` 7 Ingress — ⊥ standard equivalent; `rewrite-target` 3 — BUT the 2 "regex capture" ones = **IDENTITY NO-OP** (`/api/(?!docs\|openapi\.json)(.*)` → `/api/$1`; file comment: "Pass /api requests through without rewriting") ∴ DROP, ⊥ port (§V.62); their real content = an EXCLUSION SET; `priority: 100/50` 4 (2 host-pair); `proxy-body-size` 3 (`50m`/`10m`/`10m`); `auth-type: basic` 1 (`base-apps/kagent/mcp-ingress.yaml`); `backend-protocol: HTTPS` 1 (vcluster) → `BackendTLSPolicy`, CRD ∃; `proxy-read-timeout: 3600` 1 (vcluster)|grep `base-apps/**/*.yaml` + handover §4, 2026-07-29
R31|allow-list = the security boundary|17 `whitelist-source-range` = the ONLY external access control. `argocd`,`vault`,`backstage`,`dex`,`coroot`,`kagent` behind it; **CORRECTED 2026-07-30 (§T.63): SEVEN Ingress carry NO allow-list, ⊥ one** — `chores-tracker-backend`, `chores-tracker-frontend`, `n8n` WEBHOOK ingress, `logging/grafana`, `oncall-agent`, `vault/ingress.yaml` (`vault.local`), `vcluster-sandbox-1`. some CERTAINLY deliberate (webhook receiver ⊥ allow-listable; `vault.local` ⊥ publicly routable). `atlantis` list ⊇ GitHub webhook range (`192.30.252.0/22`…). found by the `ingress-policy` CI check, ⊥ by grep — & that check fires ONLY on CHANGED file ∴ the other 6 pass untouched. Istio path = `AuthorizationPolicy` `ipBlocks` BUT ! real client IP survive to the enforcement point — depends `externalTrafficPolicy` + proxy-protocol config. **∀ 15 list ALSO carry `10.0.0.0/8` ⊇ node `10.0.1.0/24` + pod `10.42.0.0/16` ∴ in-cluster & LAN sources ALWAYS pass — deliberate for LAN, but ⊥ portable (§B.9, §V.66)**|live cluster + handover §2,§4 + grep repo, 2026-07-29
R32|replacement options → **A CHOSEN 2026-07-29**|**A** Istio Gateway API — **DECIDED (§T.48)**, judged most production-ready; class ∃, but Istio `1.24.0` out of tested range (§V.49,§T.10) ∴ couples ingress to a postponed upgrade → §V.59, §V.60. **B** `gloo-gateway-v2` — ∃ (`gloo-system/gloo-gateway`, ClusterIP, Accepted), serves NOTHING; licence ? MOOT. **C** traefik — k3s ships it, explicitly disabled (`disable: traefik`, §C), Ingress + Gateway API ∴ annotation-preserving; MOOT. **D** maintained Ingress controller (HAProxy \| Envoy Gateway) — lowest semantic risk, ⊥ advances Gateway API; MOOT. **E** stay @ 1.35 — baseline; cost = archived internet-facing software; REJECTED|handover §5 + decision 2026-07-29
R33|pre-migration baseline|`oncall-crewai/chores-tracker-agent-tls` STUCK (`Issuing certificate as Secret does not exist`) for `chores-agent.arigsela.com` — BROKEN BEFORE the work starts ∴ ⊥ a §T.43 regression. `kubectl get certificate -A` = 23 Ready + 1 broken. curl spot-check 2026-07-29 from allow-listed addr: `argocd` 200, `vault` 307, `grafana` 302|live cluster 2026-07-29
R34|Istio ingress ⊥ need mesh enrollment|Gateway API `Gateway` @ class `istio` → its OWN Envoy Deployment; `HTTPRoute` backends = plain `Service` ∴ the 43 non-ambient ns ⊥ need enrollment for north-south to work. ambient enrollment stays OPTIONAL (buys mTLS + policy on the EAST-WEST leg only). ∴ handover §5's "2 of 45 ns ∈ mesh" ⊥ a blocker for ingress itself. allow-list lands ON the gateway pod (itself an Istio proxy) via `AuthorizationPolicy` `ipBlocks` ∴ ⊥ needs enrolled backends either. ! CONFIRM @ §T.10's landed version|istio design, ⊥ live-verified 2026-07-29 ?
R35|Istio ingress client IP|`ipBlocks` = source IP of the packet AS IT ARRIVES at the proxy ∴ correct ONLY w/ `externalTrafficPolicy: Local` (\| hostNetwork). `remoteIpBlocks` = original client IP from XFF \| PROXY protocol ∴ needs `numTrustedProxies` ∈ `meshConfig.gatewayTopology` — & ⊥ proxy ∃ in front here (§R.25) ∴ `remoteIpBlocks` = the WRONG tool & its XFF trust = §V.56's hazard. today nginx gets the real client IP FREE via `hostNetwork: true`; a Gateway Deployment @ default `externalTrafficPolicy: Cluster` is SNAT'd ∴ ∀ client → node IP → §V.61|istio.io/latest/docs/tasks/security/authorization/authz-ingress/
R37|Gateway API CRD floor & drift|live = MIXED bundles: `gateways`,`httproutes`,`grpcroutes`,`gatewayclasses`,`referencegrants` @ `v1.2.0` but **`backendtlspolicies` @ `v1.4.0`** ∴ ⊥ managed by app `istio-gateway-api` (pins `v1.2.0` @ `config/crd/standard`; BackendTLSPolicy only reached STANDARD in v1.4) ∴ ORPHANED, same class as §T.64. latest GW API = `v1.6.1` (2026-07-16; `v1.6.0` graduated TCPRoute+UDPRoute → GA). Istio's current docs install `v1.5.1`. **`referencegrants` served=[`v1beta1`] stored=[`v1beta1`] ONLY** ∴ if the target bundle drops `v1beta1` → the §R.3/§V.26 ESO trap EXACTLY: k8s ⊥ drop a version ∈ `storedVersions`. §V.63 makes ReferenceGrant load-bearing ∴ ⊥ a cosmetic upgrade|kubectl + github.com/kubernetes-sigs/gateway-api/releases + istio.io, 2026-07-30
R36|Argo Rollouts traffic routing|Istio VirtualService = NATIVE; Gateway API HTTPRoute = **PLUGIN ONLY**, ⊥ built in. live: the `chores-tracker-backend` Rollout drives canary weights via `chores-tracker-backend-vsvc` (`base-apps/chores-tracker-backend/deployments.yaml:66-70` + `virtualservice.yaml`) & `chores.arigsela.com/api/*` targets that same app. vsvc binds `hosts: [chores-tracker-backend]` (mesh short name) & nginx ⊥ mesh-enrolled ∴ whether the canary affects NORTH-SOUTH traffic today = ? UNVERIFIED|argo-rollouts.readthedocs.io/en/stable/features/traffic-management/ + repo truth 2026-07-29

## §V INVARIANTS
V1: ∀ hop → verified fs backup `/var/lib/rancher/k3s/` ∃ & restore drill passed before hop starts.
V2: ∀ hop → ∀ platform component ∈ its supported k8s matrix @ target minor, EXCEPT bounded transient per §V.27. ⊥ hop otherwise.
V3: ∀ hop → 1 minor step. ⊥ skip.
V4: control-plane hops before workers. ∀ worker hop after control-plane Ready.
V5: ∀ hop → post-gate: ∀ node Ready & ∀ Argo CD app Synced+Healthy before next hop.
V6: CNPG `postgresql-cluster` → pg_dump verified & restorable before ∀ hop.
V7: ⊥ rc/pre-release image, EXCEPT Argo CD `v3.5.0-rc2` — ACCEPTED 2026-07-28. ⊥ 3.5 GA ∃ (§R.15); `3.4.5` GA covers k8s ≤1.35 but a downgrade-across-minor carries its own risk, judged > the RC risk for a homelab. running 11d clean. ! REVISIT @ 3.5.0 GA, & if §T.42 kyverno drift traces to it.
V8: ∀ component upgrade → own commit, own sync, own rollback point. ⊥ batch.
V9: outage window announced & ≤ 15min per control-plane hop.
V10: ∀ hop → removed-API scan clean vs target minor.
V11: ∀ hop → ESO healthy & ∀ ExternalSecret `SecretSynced=True`.
V12: Istio ambient upgrade → revision/canary. ⊥ in-place istiod replace. **EXCEPTION 2026-07-30 (§T.66, decision): IN-PLACE istiod PERMITTED for THIS mesh under §V.70's protocol. rationale: (a) ambient, 2 of 45 ns enrolled ∴ blast radius bounded; (b) §R.23 — `istio-cni`+`ztunnel` ⊥ canary-able AT ALL ∴ revisioning istiod buys a safe CONTROL plane while the DATAPLANE still goes in-place per node, i.e. it removes ⊥ the actual risk; (c) istiod down ∈ ambient = config-distribution PAUSE, ⊥ dataplane outage — `ztunnel` keeps enforcing already-pushed config. ⊥ generalise: a sidecar mesh, or ≫2 ns, re-earns the canary requirement.**
V13: remediation & walk interleave WHERE a component matrix demands it. ESO was the only such case (≥`0.20` ! k8s ≥1.34, §R.4) and is DEFERRED past §T.19 (§V.48) ∴ ⊥ forcing case remains: Istio `1.30` & cert-manager `1.21` both ∈ matrix @ 1.33. ∀ hop → ∀ component @ max version its matrix allows for CURRENT minor first.
V14: ∀ control-plane hop → outage = API + Vault + CNPG. ⊥ model as API-only. all 3 restored & verified before hop declared done.
V15: k3s backup ! quiesced — stop k3s \| `sqlite3 .backup` before copy. ⊥ archive live datastore file SET: `state.db` + `state.db-wal` + `state.db-shm`. kine = WAL mode ∴ stale sidecar replays over snapshot → silent corruption.
V16: restore drill ! prove API returns @ prior version from artifact. unproven artifact ∉ backup.
V17: control-plane hop = manual, operator-driven, console access ∃. SUC scope = agents only.
V18: ∀ hop window → Argo auto-sync paused ∀ app w/ PVC. resume only after §V.5 green.
V19: ⊥ PVC ∈ prune scope, ∀ time. local-path reclaim=Delete ∴ prune = data loss.
V20: Istio upgrade → new Application per revision, old revision live until `ztunnel`+`istio-cni-node` green ∀ node. ⊥ mutate `targetRevision` in place. **SUPERSEDED for §T.10 by §V.12's exception + §V.70 (2026-07-30): `targetRevision` bumps ARE the mechanism here; the rollback lever becomes `git revert` → Argo re-sync rather than deleting a parallel Application. still binding if the §V.12 exception's conditions stop holding.**
V21: ∀ backup artifact (k3s, pg_dump, vault) ! stored ∉ cluster. artifact on `local-path` ∉ backup.
V22: ∀ hop → helm-controller reconcile of `kube-system/ingress-nginx` verified & ingress reachable post-hop.
V23: ESO upgrade ! staged. ⊥ jump `v0.11.0` → ≥`0.17.0` — `0.17.0` REMOVES `external-secrets.io/v1beta1` & 59 manifest + CRD storage version ∀ @ `v1beta1`. path: `0.16.2` (serves both) → ∀ manifest `v1` → storage migrated → ≥`0.17.0` → `2.x`. ∀ stage own gate. §T row order ≠ exec order ∴ ! follow `after §T.n` cites.
V24: Argo auto-sync paused ∀ app w/ ExternalSecret during §T.31 migration window ONLY (⊥ ∀ ESO stage). `0.16.x` webhook rewrites stored objects → drift vs git ∴ `selfHeal` fights webhook. resume once manifests @ `v1` committed (§R.7: resolution = migrate, ⊥ code fix).
V25: barman restore ! proven (scratch Cluster ← `bootstrap.recovery`) BEFORE §T.9. barman restore ⊥ operator ∴ ⊥ verifiable after operator upgrade breaks.
V26: ⊥ ESO ≥`0.17.0` until `status.storedVersions == ["v1"]` ∀ of `externalsecrets`+`secretstores`+`clustersecretstores`. manifests @ `v1` ∈ git ≠ objects @ `v1` ∈ etcd (§R.3) ∴ k8s refuses to drop `v1beta1` & existing secrets become unreadable.
V27: §V.2 exception — component ? out-of-matrix ACROSS a hop boundary iff ALL: (a) in-matrix @ CURRENT minor pre-hop, (b) upgrade → in-matrix @ target ∈ SAME maintenance window, (c) rollback artifact ∃ & drill-proven. ⊥ open-ended skew. ESO `0.19`→`0.20` = the case (§R.4).
V28: ∀ hop → Vault data backed up off-cluster & restore-proven. `vault-0` storage=`file` @ local-path pinned `k3s-control-01` ∴ node loss = ∀ secret loss ∀ ns. KMS auto-unseal protects SEAL, ⊥ data.
V29: §T.27 = HARD gate. DECIDED 2026-07-27 = RELOCATE both → `k3s-worker-01`. ⊥ §T.13+ until §T.36 & §T.37 complete. relocate = PV migration ∴ ! before walk, ⊥ retrofit mid-walk.
V30: §V.18 pause ? be lifted to reach §V.5 green — paused app ⊥ self-heal ∴ ⊥ circular wait. order: pause → hop → MANUAL sync → verify → resume.
V31: ∀ hop task ! enumerate FULL gate set incl §V.6. ⊥ bare "gate →".
V32: Istio hop plan ! explicit per-minor & ∀ intermediate ∈ k8s matrix @ CURRENT minor. Istio `1.25` = k8s ≤1.32 ∴ ∉ 1.33 ∴ ⊥ naive 1-minor walk. `1.26`+ ∈ matrix @ 1.33. **EXCEPTION 2026-07-30 — resolves the §V.64 contradiction: a component's OWN sequential walk @ a FIXED k8s minor ? pass THROUGH out-of-matrix intermediates iff ALL: (a) start version already out-of-matrix & working \| in-matrix, (b) END version ∈ matrix @ the current minor, (c) ∀ step lands in ONE campaign, ⊥ parked (§V.64), (d) §V.49's per-node `istio-cni-node`+`ztunnel` DS check green BEFORE each next step. skew is then TRANSIENT & bounded by the campaign ≠ the open-ended skew this invariant forbids — same shape as §V.27 for hop boundaries. §T.10 = the case: `1.25`-`1.28` ∀ ∉ matrix @ k8s 1.35, `1.29`+`1.30` ∈.**
V33: ∀ quiesce of GitOps-managed workload (scale→0 \| pod delete) → ! suspend its Argo app FIRST, resume after. `selfHeal: true` + replicas ∈ git ∴ Argo rescales mid-operation → torn artifact \| silent revert (§B.2). ALSO ! suspend PARENT app-of-apps (`master-app`) — child `syncPolicy` ∈ git ∴ parent re-sync silently restores `automated` mid-op (§B.4). ⊥ patch child alone. applies §T.35, §T.36, §T.37.
V34: ∀ pod mounting a node-pinned `local-path` PV → ! carry toleration ∀ taint on that node + explicit resource limits. `k3s-control-01` taint = `node-role.kubernetes.io/control-plane:NoSchedule`; kyverno `require-resource-limits` = Audit (warns, ⊥ blocks). ⊥ toleration → Pending forever, ⊥ 2 workers (∉ PV affinity). applies §T.35, §T.36, §T.37.
V35: Vault backup ⊇ KMS key. `alias/vault-auto-unseal` deleted \| AWS acct lost → ∀ vault artifact undecryptable ∀ time, however perfect the bytes. ∴ key ! carry deletion protection & its policy backed up ALONGSIDE artifact. ⊥ treat key as adjacent to the backup.
V36: ⊥ delete PVC w/ reclaim=`Delete` unless restore-proven backup ∃ & verified. deliberate delete = §V.19 hazard by another route. applies §T.36.
V37: post-§T.36+§T.37 → §V.14 scope MOVES: control-plane hop = API only; `k3s-worker-01` hop = Vault + CNPG outage. ∀ hop task ! re-cite before exec. §T.18 = highest-risk hop post-relocate.
V38: §T.37 scale-down gate — ! assert `postgresql-cluster-2` = primary BEFORE `instances` 2→1. CNPG drops highest-serial NON-primary & ⊥ targetable ∴ failed switchover → silent revert to control-plane.
V39: ∀ PVC ∉ Argo prune scope — `Prune=false` annotation \| app `prune: false` where chart ⊥ template annotations. enforced @ `tests/k3s-upgrade/test_prune_scope.py`. 10/21 WERE exposed 2026-07-27 ∴ §V.19 was false since inception.
V40: §T.36 order ! = commit `nodeSelector` → Argo sync → suspend → scale 0 → delete PVC → helper pod (nodeSelector + §V.34 toleration) provisions & receives restore → delete helper → scale 1 (STS adopts `vault-data-vault-0`). ⊥ start Vault on empty PVC — `WaitForFirstConsumer` ∴ scaling up to create the PV self-initialises Vault before restore.
V41: §T.39 runbook ! ∃ before §T.17. §V.9 "announced" ⊥ satisfiable w/o it.
V42: ∀ node drain w/ CNPG single-instance on it → BLOCKED by PDB (`enablePDB=true`, disruptionsAllowed=0, §R.10). PDB selects `instanceRole=primary` ∴ constraint FOLLOWS the primary, ⊥ eliminated by relocation. §T.37 unblocked §T.17 but MOVED the block to `k3s-worker-01` ∴ §T.18. mitigation @ hop time: scale→2 \| `enablePDB: false` \| `nodeMaintenanceWindow`.
V43: relocate CNPG across nodes = 3 PHASE. (a) BROADEN selector so CURRENT node still matches (nodeAffinity `In [old,new]` \| drop selector + cordon every node except target) → restart = no-op. (b) cordon old → `instances`+1 → auto-switchover → assert primary (§V.38) → scale back → uncordon. (c) NARROW to target host. ⊥ narrow before relocate — restarted primary ⊥ schedulable off its own PV (§B.5).
V44: ∀ live patch of a NESTED MAP (`nodeSelector`, labels, annotations) → `kubectl patch --type json` REPLACE. `--type merge` MERGES maps ∴ old keys survive → impossible conjunction (§B.5). ! verify the resulting object before proceeding.
V45: after ANY out-of-band `kubectl patch` on an Argo-managed resource, `kubectl.kubernetes.io/last-applied-configuration` goes STALE ∴ Argo client-side 3-way merge yields UNION of stale + desired, ⊥ replacement. before resuming auto-sync ! BOTH: (a) refresh & CONFIRM `status.sync.revision` == merged commit — Argo ? sync a CACHED older rev, (b) `kubectl apply -f <git manifest>` to repair last-applied \| set `ServerSideApply=true`. ⊥ resume on faith (§B.6).
V46: ⊥ hop → 1.36 while `ingress-nginx` ∈ cluster. repo archived, `v1.15.1` terminal, supports ≤1.35 (§R.13) ∴ ⊥ future version. 1.36 ! preceded by §T.43 (ingress replacement; engine = Istio Gateway API, §R.32). ∀ other 1.35-capped component ? ship 1.36 support later; this one ⊥ can.
V47: §V.5 ⊥ absolute. controller-mutated fields (webhook `caBundle`, SA tokens, defaulted spec) drift PERMANENTLY vs git ∴ "∀ app Synced" unreachable in a real cluster. REVISED gate: ⊥ UNEXPLAINED drift — ∀ OutOfSync app ! have a documented benign cause + `ignoreDifferences` covering it, else it blocks. ⊥ blanket `ServerSideApply` as the remedy (§R.19).
V48: **RETIRED 2026-08-03 — ESO now @ `2.8.0`, current (§T.6/§T.31/§T.32/§T.33).** historical: `v0.11.0` DEFERRED past §T.19 (decision 2026-07-28) ∴ ran ~4 minors out of matrix @ 1.35; ACCEPTED because break = secret REFRESH stops while existing `Secret` PERSIST ∴ gradual degrade, ⊥ outage. that judgement held — 40/43 throughout the deferral & the whole 6-stage walk. NB ESO REMAINS a 1.36 blocker (§R.18): `2.7` = newest w/ a documented matrix & caps @ **1.35**; ⊥ release supports 1.36 ∴ now a WAIT on upstream, ⊥ technical debt
V51: ∀ repeated pod-sandbox failure on a node LEAKS a host-local IPAM reservation. flannel allocates the IP FIRST, the chained plugin fails AFTER ∴ the reservation in `/var/lib/cni/networks/cbr0/<ip>` is never released. failure is SILENT + DELAYED: the node keeps running existing pods & only stops being able to CREATE them, & a stuck CronJob pod ∉ any Argo app ∴ ⊥ visible to a §V.5 app-level check. ∴ after ANY sandbox-failure incident ! AUDIT the node: `ls /var/lib/cni/networks/cbr0 | grep -c ^10\.` vs live pods. PRUNE = delete ONLY files whose IP ∉ live pod IPs AND whose container-id ∉ `crictl pods -q`+`ps -aq` (KEEP on EITHER match; ⊥ wipe the dir — live pods would collide). ! tar the dir first. §B.8 = 219/254 leaked, node ⊥ create ANY pod for 14h.
V50: k3s upgrade ROTATES `/var/lib/rancher/k3s/data/<hash>/` & repoints the `data/current` symlink ∴ ∀ third-party CNI binary living under `data/current/bin/` is ORPHANED by the hop. istio-cni DS mounts `cni-bin-dir` hostPath = `data/current/bin/`, RESOLVED BY THE KUBELET AT POD-CREATE ∴ the ALREADY-RUNNING pod keeps writing into the OLD hash dir — it DOES reinstall (binary mtime == hop time) just into the wrong place — & its presence-check passes on a DANGLING symlink ∴ it ⊥ EVER self-heal. `data/cni/istio-cni -> data/current/bin/istio-cni` then dangles → containerd `failed to find plugin "istio-cni"` → ∀ NEW pod sandbox on that node fails, incl. coredns ∴ CLUSTER DNS DOWN ∴ cascade into ∀ workload doing name resolution. ∴ ∀ k3s hop on node N ! RESTART the `istio-cni-node` pod on N as the FIRST action after k3s returns, BEFORE any §V.5 assessment. VERIFY: `data/current/bin/istio-cni` ∃ AND `readlink -f data/cni/istio-cni` resolves INTO the new hash dir. ⊥ trust DS Ready count (§B.7: 3/3 Ready for the whole outage). §T.46 = durable fix, §T.47 = gate check.
V49: Istio `1.24.0` DEFERRED past §T.19 (decision 2026-07-28). ALREADY out of tested range @ 1.33 (§R.24) & working; only 2 ns ∈ ambient (`chores-tracker`,`-frontend`). RISK ≠ ESO: `istio-cni` = CNI plugin chained into node networking ∴ failure ? break pod create on a node, ⊥ merely degrade. ∴ ∀ hop → CHECK `istio-cni-node` + `ztunnel` DS Ready ∀ node & both ambient ns still routing. ⊥ proceed to next hop if degraded. **DEFERRAL ENDED 2026-07-29 — §T.48 chose Istio as the ingress engine ∴ §T.10 = PREREQUISITE, ⊥ optional. see §V.59, §V.60.**
V52: ⊥ remove \| disable `ingress-nginx` until ∀ Certificate @ `letsencrypt-prod` (20 of 24, HTTP-01 via `ingress.class: nginx`, §R.27) migrated → `letsencrypt-route53` (§R.28) & renewal PROVEN. break = SILENT — existing certs keep serving, renewal fails ~30d later, long after cutover declared done. cert-manager's own solver Ingress = also a consumer.
V53: ∀ host cutover → access-control equivalence PROVEN by a real request from a NON-allowlisted address → ! denied. reading YAML ⊥ proof (§R.31: client IP has to survive `externalTrafficPolicy` + proxy-protocol to reach the enforcement point). allow-list = only external access control & cluster scanned hourly (§R.25) ∴ a host cutting over without it is exposed the same day.
V54: ingress migration = host-by-host, old `Ingress` LIVE until the replacement is proven for that host. ∀ Argo app `prune: true` (§C) ∴ removing an `Ingress` from git deletes it from the cluster on next sync → gap. ⊥ big-bang.
V55: `:80`/`:443` @ `10.0.1.50` ⊥ shareable — ingress-nginx = `hostNetwork: true`, pinned `k3s-control-01`. ∴ a replacement that also wants hostNetwork ⊥ co-run. cutover strategy (distinct ports + DNS/firewall flip \| different node \| hard window) ! DECIDED BEFORE ∀ manifest written.
V56: ∀ proxy in front of the cluster → `trusted-proxies` scoped to THAT proxy's addresses ONLY. ⊥ `10.0.0.0/8` — ⊇ pod net `10.42.0.0/16` ∴ ANY pod spoofs its client IP past ∀ allow-list. the removed Cloudflare config had exactly this hole (`9e53db8`). ⊥ reintroduce `X-Forwarded-For` trust without it.
V57: `chores.arigsela.com` + `weather-kitchen.arigsela.com` each serve 2 app via `priority` 100/50 + regex `rewrite-target: /api/$1`. `URLRewrite` = prefix ONLY, ⊥ capture group (§R.30) ∴ paths ! re-expressed as prefix match & BOTH `/api/*` and `/*` verified per host. highest subtle-break risk. **SHARPENED by §V.62 — the path is an EXCLUSION SET ⊥ a plain prefix, & the rewrite is a no-op.**
V58: retiring `ingress-nginx` ! update `scripts/hop-verify.sh` `check_ingress` + §V.22 IN THE SAME change. the gate hard-asserts `HelmChart kube-system/ingress-nginx` ∃ + DS Ready (§T.29) ∴ a SUCCESSFUL migration turns the cluster gate RED & every future hop reads as failed.
V59: §T.10 (Istio → ≥`1.30`) ! COMPLETE BEFORE ∀ north-south traffic moves onto an Istio Gateway. ORDERING ASYMMETRY: today Istio carries 0 north-south traffic (§R.26) ∴ the 6-step upgrade disrupts 2 ambient ns & nothing else. AFTER cutover the SAME upgrade = a 21-hostname production event, & `istio-cni`+`ztunnel` steps are IN-PLACE & ⊥ canary-able (§R.23). ∴ upgrade while it is cheap. ⊥ build ingress on `1.24` intending to upgrade later.
V60: ingress target = Istio ≥ `1.30`, ⊥ `1.29`. §T.43 exists to unblock 1.36 (§V.46); Istio `1.29` = k8s 1.31-1.35 (§R.24) ∴ landing ingress on `1.29` SWAPS one 1.35-capped ingress for another & 1.36 stays blocked — the migration would buy nothing but maintained software. **CONFIRMED 2026-07-29 × 2 source (§R.24): `1.30` = k8s 1.32-1.36 ∴ reaches 1.36 — the `?` is CLOSED.** FURTHER: only `1.29`+`1.30` supported upstream & `1.29` EOL ~2026-08 ∴ `1.30` = the only non-EOL target.
V61: Istio ingress Gateway ! `externalTrafficPolicy: Local` (\| hostNetwork) BEFORE ∀ `AuthorizationPolicy` `ipBlocks` rule is trusted. `ipBlocks` = packet source IP (§R.35) ∴ SNAT → ∀ client looks like the node. **CORRECTED 2026-07-29 (§B.9): this is ⊥ a lockout HERE — `10.0.0.0/8` sits in ∀ 15 live allow-list ALREADY ∴ the node address MATCHES → SILENT FULL-OPEN, ∀ host 200, ⊥ symptom. see §V.66.** ⊥ `remoteIpBlocks` here — it needs `numTrustedProxies` XFF trust & ⊥ proxy ∃ in front (§R.25). ! prove BOTH directions per host: allow-listed source → 200, blocked source → 403.
V62: `chores.arigsela.com` + `weather-kitchen.arigsela.com` `/api` path = an EXCLUSION SET, ⊥ a prefix. `(?!docs\|openapi\.json)` ∴ `/api/docs` + `/api/openapi.json` → FRONTEND, rest of `/api/*` → backend. ! exact-match route (outranks prefix in Gateway API precedence) for the 2 exclusion. the `rewrite-target: /api/$1` = IDENTITY no-op ∴ DROP it, ⊥ port it (§R.30).
V63: ∀ Gateway listener `certificateRefs` crossing a namespace ! a `ReferenceGrant` ∈ the TARGET ns. 18 TLS secret ∈ 20 ns today ∴ EITHER 20 ReferenceGrant OR cert issuance relocated into the gateway ns. ⊥ assume a Gateway reads app-ns Secret — listeners come up SILENTLY certless.
V64: §T.10 = ONE campaign, ⊥ parkable. Istio `1.25`-`1.28` ∀ ∉ matrix @ k8s 1.35 (§R.24) ∴ a half-done walk sits FURTHER out of matrix than the nginx status quo it retires. ⊥ start unless all 6 step land in the same campaign.
V65: `chores-tracker-backend` canary weights = an Istio VirtualService (Rollouts NATIVE). Rollouts drives Gateway API via PLUGIN only (§R.36) ∴ ∀ re-route of `chores.arigsela.com/api/*` ! EITHER keep the VirtualService leg, OR install the Rollouts Gateway API plugin, OR retire the canary DELIBERATELY. ⊥ leave it dangling.
V67: ∀ internet-reachable app w/o an IP allow-list ! stay on a security-supported patch line. IP restriction = the compensating control ∀ everything else (§R.31); `grafana.arigsela.com` has NONE ∴ its ONLY controls = the login form + the patch level. `11.3.1` ran ~20 month unpatched (CVE-2025-3260 high, +2 medium) on a host scanned hourly (§R.25). ⊥ let the one host without a network control also be the stalest.
V70: in-place Istio step protocol (governs the §V.12/§V.20 exception). ∀ STEP = exactly 1 minor, components in §R.23 order: `base`(CRD) → `istiod` → `cni` → `ztunnel` → waypoints. AFTER EACH STEP, ALL ! hold before the next: (a) §V.49 — `istio-cni-node`+`ztunnel` DS Ready ∀ 3 node; (b) BOTH ambient ns (`chores-tracker`,`chores-tracker-frontend`) still routing; (c) both waypoint `Gateway` Programmed=True; (d) ⊥ pod stuck ContainerCreating ∀ node (§B.7's signature — DS Ready ⊥ sufficient, it read 3/3 THROUGHOUT that outage). ROLLBACK = revert the chart `targetRevision` commit → Argo re-sync; ! proven on step 1 BEFORE step 2 is attempted. ⊥ advance a minor on an amber check. ztunnel step disrupts ambient traffic per node (§R.23) ∴ expect it, ⊥ diagnose it.
V69: grafana GitHub OAuth identity filter **FAILS OPEN by default**. `role_attribute_strict` default = `false` ∴ a login matching ⊥ role gets `auto_assign_org_role` (Viewer) & IS ADMITTED ∴ one typo'd JMESPath → ∀ GitHub account on earth reads the dashboards, SILENTLY, on the one host w/ ⊥ IP allow-list (§R.31) & scanned hourly (§R.25). ! BOTH gates, both fail-CLOSED: (a) `role_attribute_strict = true`, (b) `allow_sign_up = false` — the 2nd admits ONLY users already ∈ grafana's DB ∴ ⊥ depends on the expression being right. `allowed_organizations` ⊥ available until §T.61 (personal account, ⊥ org). ORDERING (§B.10): gate (b) ⊥ closable from a cold start — grafana links an OAuth identity by `auth_module`+`auth_id` created BY signup, & `oauth_allow_insecure_email_lookup=false` default ∴ pre-creating a user by email ⊥ link. ∴ bootstrap signup ON → link → close. adding ∀ future user = same flip-flop. **STATUS 2026-07-30: BOTH GATES PROVEN BY OBSERVATION.** gate (a) — `role_attribute_path` inverted to a login that ⊥ match → GitHub login REFUSED w/ `[oauth.role_attribute_strict_violation]` + 0 `Successful Login` (§T.62). gate (b) — unlinked `arigsela` denied "signup is not allowed" 2026-07-29 (§B.10) & ∀ other GitHub account is in that same unlinked state. NB the invert route WORKS w/ gate (b) closed — a LINKED user resolves by `auth_module`+`auth_id` & never consults signup ∴ gate (a) tests in ISOLATION; only the 2nd-ACCOUNT route was foreclosed.
V68: dex GitHub connector carries **⊥ `orgs:` restriction** (personal account, `base-apps/dex/configmap.yaml:42-53`) ∴ ANY GitHub account authenticates to dex. today safe ONLY because (a) Vault's oidc role binds `bound_claims` to one login & (b) `dex.arigsela.com` is IP-allowlisted (403 from LTE, proven §T.50). ∴ ∀ NEW dex client ! enforce its OWN identity allow-list BEFORE dex's allow-list is loosened. ⊥ loosen dex first — that opens ∀ GitHub user to ∀ client simultaneously.
V66: ∀ allow-list CIDR ! RE-SCOPED at translation, ⊥ COPIED. ∀ 15 live list carry `10.0.0.0/8` ⊇ node net `10.0.1.0/24` & pod net `10.42.0.0/16` (§R.31) ∴ under SNAT (§V.61) the gateway's OWN source address matches the rule → ∀ internet client ALLOWED on `argocd`,`vault`,`dex`,`backstage`… & ∀ host returns 200 ∴ ⊥ symptom to notice. ! the translated `ipBlocks` set EXCLUDE node+pod CIDR; a LAN rule, if wanted, written SEPARATELY & scoped `10.0.1.0/24`, ⊥ `/8`. ⊥ port an allow-list verbatim.

## §T TASKS
id|status|task|cites
T1|x|write `scripts/k3s-backup.sh` — quiesce k3s, `sqlite3 .backup`, tar, checksum, ship off-cluster|V1,V15,V21,I.cmd
T2|x|write `scripts/k3s-restore.sh` + drill on throwaway VM: artifact → API up @ prior version|V1,V16,I.cmd
T3|x|`scripts/pg-backup.sh` pg_dump `postgresql-cluster` → off-cluster, verify restore|V6,V21,I.cmd
T4|x|deprecated/removed-API scan vs 1.34,1.35,1.36 (`pluto` \| `kubent`)|V10
T5|x|DECIDED 2026-07-28: KEEP `v3.5.0-rc2`. ⊥ 3.5 GA ∃; downgrade-to-3.4.5 risk judged > RC risk. §V.7 amended to record it. REVISIT @ 3.5.0 GA \| if §T.42 kyverno drift traces to the RC|V7
T6|x|DONE 2026-08-03. ESO stage 1: chart `0.11.0` → `0.16.2` — the LAST version serving `v1beta1` alongside `v1` (§R.2). 5 minors in 1 hop, against upstream's one-minor-at-a-time advice: accepted because ∀ intervening version serves `v1beta1` ∴ ⊥ structural change before §T.31, & §V.48's failure mode = refresh stops / existing Secret persist. GATE OPENED: `v1` ⊥ served AT ALL @ `0.11.0` ∴ §T.31's manifests were unappliable until this landed|V11,V8,V23,V24,V48
T7|x|DONE 2026-08-03 w/ the walk. ∀ 43 ExternalSecret verified after EACH stage: 40 Ready / 3 failing, unchanged from baseline. the 3 = pre-existing `SecretSyncedError` "could not get secret data from provider" ∈ `langflow-ide`, `oncall-crewai`, `whoami-test` — missing Vault path, ⊥ ESO fault. live refresh confirmed post-`2.8.0` (`refreshTime` advancing, `SecretSynced`). Vault k8s-auth role intact ∀ 20 ns|V11
T8|.|Vault `1.18.1` → current stable. NB StatefulSet `updateStrategy: OnDelete` ∴ ! manual `delete pod vault-0` after sync|V8
T9|x|DONE 2026-07-28: CNPG operator `1.24.1`→`1.29.1` in 5 sequential steps (§R.22, ⊥ skip minors). chart `0.22.1`→`0.23.2`→`0.25.0`→`0.26.1`→`0.27.1`→`0.28.3`. CVE-2026-44477 (CVSS 9.4) FIXED — patched in 1.29.1/1.28.3. ∀ step verified healthy + data unchanged (`n8n` 55 tbl, `chores_tracker` 7 tbl, `chores` 3 rows). restarts ≪ estimate: back healthy inside one 15s poll each. now ∈ matrix @ k8s 1.33-1.35|V2,V6,V8,V25,R.22
T10|x|**DONE 2026-07-31 — 6/6 steps, `1.24.0` → `1.30.3`.** `1.25.5`→`1.26.8`→`1.27.9`→`1.28.10`→`1.29.6`→`1.30.3` (latest patch per minor), ONE campaign per §V.64, in-place per §V.12's exception, §V.70 gate green after EVERY step: both dataplane DS 3/3 updated=3 ∀ node, `chores` 200 on both path, both waypoint `Programmed=True`, 0 `ContainerCreating`. waypoints followed to `proxyv2:1.30.3` unaided. 4 component moved per step in ONE PR — the EXISTING sync-waves (base -2, istiod -1, cni 0, ztunnel 1) already encode §R.23's order ∴ splitting would only skew the mesh between merges. ⊥ outage observed ∀ step. **FINDING: the ztunnel DS RENAMED mid-campaign — `ztunnel`(1.24) → `istio-ztunnel`(1.25) → `ztunnel`(1.26)** ∴ a gate check hardcoding the name reads MISSING (= looks catastrophic) \| silently finds nothing; Argo pruned the stale DS correctly (verified 1 ztunnel pod/node, ⊥ doubled dataplane). ∴ §V.49/§V.70/`hop-verify.sh` ! read LIVE resource names, ⊥ a remembered list. §V.59 now SATISFIED ∴ §T.55/§T.56 may carry north-south traffic|V12,V20,V32,V49,V59,V60,V64,V70
T11|x|DONE 2026-07-31 w/ §T.10. `istio-cni-node` + `ztunnel` DS 3/3 `updated=3` ∀ 3 node @ `1.30.3`; ⊥ old revision to retire (in-place per §V.12's exception ∴ ⊥ parallel revision ever existed). GatewayClass `istio` Accepted, controller `istio.io/gateway-controller` — the class §T.55 will use|V12,V20,V49
T12|x|DONE 2026-07-28 matrix audit → §R.13-§R.18. blockers: ingress-nginx TERMINAL, kyverno ≤1.35, ArgoCD ⊥ 3.5 GA. clear: cert-manager→v1.21, Istio→1.30. RE-SCOPED to 1.35 (§R.20): ingress-nginx + cert-manager need NOTHING|V2,V46
T13|x|DONE 2026-07-28: `base-apps/system-upgrade-controller.yaml` Argo app. SUC `v0.20.1`, Synced/Healthy|V29,I.file
T14|x|DONE 2026-07-28: official v0.20.1 manifests + CRD, sync-wave -2/-1/0. `plan-agent.yaml` agents ONLY, ⊥ `plan-server.yaml`. `drain` ABSENT (local-path node-pinned), `cordon: true`, `concurrency: 1`, `version` pinned ⊥ channel. guards @ `tests/k3s-upgrade/test_suc_plan.py`|V3,V17,I.file
T15|~|`k3s-worker-02` labelled `k3s-upgrade=true` for §T.16. `k3s-worker-01` deliberately UNLABELLED until the real hop (hosts Vault+CNPG). `k3s-control-01` ⊥ labelled ∀ time (§V.17)|V17,I.node-label
T16|x|DONE 2026-07-28 dry run on `k3s-worker-02`: job created t+0s → cordon t+15s → Complete + uncordon t+31s. ⊥ NotReady ∀ moment. 21 workloads survived, other nodes untouched. NB job logged "Binary already been replaced" & exit 0 ∴ k3s never restarted — mechanism proven, real hop NotReady window still UNMEASURED|V4,V17
T17|x|DONE 2026-07-28 20:12-20:45Z. cold backup 49MB verified × 3 copies (node+laptop+S3) → `INSTALL_K3S_VERSION=v1.34.9+k3s1 INSTALL_K3S_EXEC="server --write-kubeconfig-mode=644"`. installer REGENERATES the unit ∴ args ! be re-supplied; `disable: traefik` ∈ `/etc/rancher/k3s/config.yaml` ∴ survives. API back <1min, unit diff IDENTICAL. HIT §B.7 (istio-cni orphaned) ∴ §V.9 = 27m27s > 15m bound. data path INTACT: CNPG primary pod ⊥ restart (58min uptime across hop), Vault unsealed, ESO 40/43. post-hop drift = the known 4, ⊥ new|V1,V2,V3,V6,V14,V17,V27,V28,V31,V50
T18|x|DONE. `k3s-worker-02` 2026-07-28 21:02-21:05Z (§V.50 confirmed absent-binary BEFORE damage, restart fixed it in ~10s vs §B.7's 27min = the value of the diagnosis). `k3s-worker-01` 2026-07-29 11:29-11:31Z, 60s to hop. §V.50 applied AUTOMATICALLY the instant the node reported the new version ∴ ⊥ gap. RESULT: CNPG primary pod `postgresql-cluster-2` age 15h ACROSS the hop = ⊥ restart AT ALL; `vault-0` restart was from the pre-hop cold backup, ⊥ the hop. ∴ predicted Vault+CNPG outage (§V.37) DID NOT MATERIALISE — pods survive a k3s agent restart (containerd shims outlive it), confirmed on ∀ 3 node. ALL 3 NODES @ `v1.34.9+k3s1`. NB label left ON worker-01 ∴ §T.19 ? hop both workers via SUC. HISTORIC ↓|V4,V5,V6,V17,V31,V37,V42,V50
T19|x|DONE 2026-07-29 11:49-12:04Z. §G TARGET REACHED — ∀ 3 node @ `v1.35.6+k3s1`. control-plane manual 11:49-11:52, workers SUC sequential 11:56-11:59. §V.50 applied WITHIN ~10-15s ∀ node ∴ §B.7 + §B.8 PREVENTED, ⊥ recovered-from: IPAM steady 34/36/43 (⊥ leak), ⊥ pod wedged, repo-server 0 restart, coredns ⊥ even recreated. CONTRAST §T.17: 18min orphan → 219/254 IP leaked → DNS down → 27min. §V.9 = 15m9s BUT that = elapsed for the WHOLE 3-node sequence incl. settle polling, ⊥ an outage — real disruption = the API restart alone (~1min), ⊥ workload impact. §V.11 = 40/43 UNCHANGED @ 1.35 ∴ §V.48's ESO deferral still holds|V3,V5,V6,V17,V27,V28,V31,V50,V51
T20|x|RESOLVED 2026-07-28 (§R.18): 1.36 blocked by 4 components; `ingress-nginx` TERMINAL ∴ permanent. → target = `v1.35.6+k3s1`, §G retargeted|V2,V46
T21|.|FOLLOW-ON (⊥ this spec's target): hop → `v1.36.2+k3s1`. gated §T.43 + CNPG/ESO/kyverno shipping 1.36 support|V2,V3,V46
T22|.|confirm local kubectl v1.36.2 now in-skew|-
T23|.|update `index.md` + `docs/` topology w/ landed versions|-
T24|.|follow-on: spec 3-server embedded etcd HA conversion|-
T25|x|DONE 2026-07-28: `scripts/argo-sync-window.sh scope\|pause\|resume`. suspends `master-app` FIRST (§B.4) then ∀ PVC app; scope = UNION of tracked-PVC apps + `volumeClaimTemplates` STS (the latter alone caught `vault`+`openshell`). records prior policy → exact restore. resume ⊥ gated on Synced (§V.30)|V18,V30,V33
T26|x|prune-scope audit: 10 PVC WERE prunable (§V.19 false since inception). fixed — `Prune=false` ∀ 9 manifest + `prune: false` @ atlantis app (chart 6.1.0 ⊥ template PVC annotations). `tests/k3s-upgrade/test_prune_scope.py` = regression guard. NB `lg-agents/orchestrator-data` orphaned (⊥ app) → §T.40|V19,V39
T27|x|DECIDED 2026-07-27: RELOCATE both → `k3s-worker-01` (97GB disk, ⊥ pressure). → §T.36 + §T.37. rationale: retires single failure domain, hops become API-only|V14,V29
T28|x|DONE 2026-07-28: artifacts shipped → `s3://mysql-backups-asela-cluster/{pg,vault}/` w/ checksums, round-trip download+verify PASSED. `k3s/` prefix awaits the first on-node backup (§T.17). KMS `alias/vault-auto-unseal` given `lifecycle.prevent_destroy` — 30d deletion window already ∃; this blocks the likelier accident (terraform destroy/replace)|V21,V35,I.store
T29|x|DONE 2026-07-28: `scripts/hop-verify.sh gate` checks helm-controller `HelmChart kube-system/ingress-nginx` + controller Ready. NB controller = DaemonSet ⊥ Deployment — first version of the check queried Deployments, found none & called a healthy ingress broken|V22
T30|x|DONE 2026-07-28: §V.9 window = `hop-verify.sh watch --since <epoch of k3s stop>` → green when ∀ node Ready & ⊥ unexplained drift. compares vs the 15min bound. measuring from later, or ending @ API-up, would flatter the number|V9
T31|x|DONE 2026-08-03. 55 file / 31 `ExternalSecret` + 25 `SecretStore` → `v1`. **§V.24's PREDICTED DRIFT DID ⊥ MATERIALISE** — ∀ 43 live object converted to `v1` on read & **0 app went OutOfSync**, because Argo NORMALISES apiVersion when comparing ∴ the auto-sync pause §V.24 demands was unnecessary. ⊥ migrated: 2 `v1alpha1` ref ∈ `crossplane-compositions/composition-application.yaml` — `PushSecret`, a DIFFERENT CRD still storing `v1alpha1`|V23,V24,V26,V48
T32|x|DONE 2026-08-03. stage 3 (out-of-band, ⊥ expressible ∈ git) + stage 4 → `0.17.0`. PROTOCOL: snapshot 43 ES + 30 SS + ∀ CRD → disk; rewrite ∀ 73 object (annotate+un-annotate) forcing re-store @ `v1`; VERIFY `storedVersions` ⊥ self-pruned; patch `status.storedVersions` → `["v1"]`. **2 SPEC ERRORS FOUND: (a) §V.26 named 3 CRD — there are FOUR, `clusterexternalsecrets` also carried `v1beta1`; (b) k8s docs imply the API server prunes `storedVersions` once objects migrate — IT DOES ⊥, it appends & never removes ∴ the manual patch is MANDATORY.** post-prune verified: 43 readable, write test OK, 40/43 Ready|V23,V26,V11,V48
T33|x|DONE 2026-08-03. `0.17.0` → `0.20.4` → **`2.8.0`** (current). breaking changes CHECKED ⊥ assumed: `1.0.0` documents none; `2.0.0` removes ONLY the Alibaba + Device42 provider & ∀ 30 SecretStore here use `vault`. NB the `0.20.4` chart REINTRODUCES a `v1beta1` CRD entry — BENIGN: `served=false`, `deprecated=true`, server-side dry-run confirms objects ⊥ creatable @ that version, `storedVersions` stays `["v1"]`. §V.11 held @ 40/43 @ EVERY checkpoint of ∀ 6 stage|V23,V13,V11,V48
T34|x|DONE 2026-07-28: barman restore PROVEN. scratch ns + CNPG `bootstrap.recovery` ← `s3://mysql-backups-asela-cluster/postgresql/` (serverName `postgresql-cluster`) → healthy in ~2min. data verified vs source: sizes exact (`n8n` 290MB, `chores_tracker` 8260kB), `chores_tracker` rows 3/1/3/3 MATCH, `n8n.invalid_auth_token` 2/2 MATCH. torn down, PV reclaimed. §V.25 satisfied ∴ §T.9 unblocked|V6,V25
T35|x|write `scripts/vault-backup.sh` + drill: `vault-0` `file` storage → off-cluster, restore ! prove unseal + secret read. ⊥ backup ∃ today ∴ FIRST, before ∀ other `.` task|V28,V21,I.cmd
T36|x|DONE 2026-07-28: `vault-0` relocated `k3s-control-01` → `k3s-worker-01`. outage 2m24s (14:39:29-14:41:53Z). new PV `pvc-35030e9f` @ worker-01. unseal via awskms verified + live ESO read `refreshTime` 14:42:12Z `SecretSynced` (§V.28). ROLLBACK: old PV `pvc-0741ca81` = `Released`+`Retain` @ control-01 `/var/lib/rancher/k3s/storage/pvc-0741ca81-..._vault_vault-data-vault-0`; artifact `vault-backup-20260728T143928Z-T36.tar.gz`|V28,V29,V33,V34,V36,V40
T37|x|DONE 2026-07-28: `postgresql-cluster` relocated → `k3s-worker-01` via §V.43 3-phase. primary now `postgresql-cluster-2` @ worker-01, 1/1 healthy, `n8n` 55 tbl + `chores_tracker` 7 tbl verified. auto-switchover fired as §R.9 predicted. ⊥ outage beyond 2 expected restarts. ROLLBACK: PV `pvc-57ffc455` = `Released`+`Retain` @ control-01; dump `pg-...20260728T152040Z.sql.gz`|V6,V29,V33,V38,V42,V43,V44
T38|.|post-relocate: `k3s-control-01` ⊥ host stateful. amend §C pinning lines + §V.14 scope per §V.37 — control-plane hop now API-only, `k3s-worker-01` hop = Vault+DB outage|V14,V29,V37
T39|x|DONE 2026-07-28: `docs/plans/k3s-1.36-upgrade-plan.md` — runbook, outage comms table, per-hop gate + rollback, §B.1-§B.6 hard-won specifics. §V.9 "announced" now satisfiable|V9,V41,I.doc
T40|.|`lg-agents/orchestrator-data` PVC tracked by app `lg-agents` that ⊥ ∃. orphaned ∴ ⊥ prunable, but ∉ GitOps. decide: adopt \| delete \| document|V19
T41|.|install `kubectl cnpg` plugin — fallback manual switchover lever for §T.37 if auto-switchover ⊥ fire (§R.12). before §T.37|V38,R.12
T42|~|drift 8→2. FIXED: master-app (`recurse: false`), openshell-secrets (ESO webhook defaults), istio-base+istio-istiod (8 fields: istiod `caBundle`+`failurePolicy`, 6 API defaults), openshell (13 API defaults), argo-rollouts (`preserveUnknownFields`+`conversion`). REMAINING: `kagent-secrets` = operator copies Argo `tracking-id` onto a generated SA ∴ needs kagent fix ⊥ ignore rule; `kyverno` = chart renders `metadata.labels: {}` on 11/22 CRD (upstream bug) — rule applied & stable CLI shows ⊥ diff, but `v3.5.0-rc2` controller still flags. retest after §T.5|V5,V47
T43|x|**DONE 2026-07-31.** `ingress-nginx` REPLACED by the Istio ingress Gateway. 18 hostname, 19 listener, 20 HTTPRoute, deny-by-default `AuthorizationPolicy`, ⊥ nginx pod remain. ∀ host matched its §T.50 baseline code @ cutover. §V.46 SATISFIED ∴ 1.36 (§T.21) unblocked. decomposed → §T.48-§T.57 + §T.63-§T.68|V46,V52,V53,V54,V55,V59,V60,R.13,R.26,R.29,R.32
T44|.|kyverno `v1.17.1` → `v1.18.x` (chart `3.8.2`). ADVISED ⊥ required: chart allows ≥1.25 (§R.21) but `v1.18` = the version TESTED @ 1.33-1.35 (§R.16). kyverno = admission webhook ∴ break blocks ∀ pod create|V2,V8
T45|.|diagnose `atlantis` (Progressing) + `whoami-test` (Degraded). both workloads 1/1 Running for 19d/7d ∴ stale health ⊥ real failure, but cause UNDIAGNOSED ∴ `hop-verify.sh` tolerates them & warns every run. §V.47 wants a documented cause, ⊥ an allow-list entry|V47,V5
T46|.|DURABLE fix for §V.50: point istio-cni `cniBinDir` → `/var/lib/rancher/k3s/data/cni` (STABLE dir = containerd's `bin_dir`; k3s refreshes only ITS OWN symlinks there & left the `istio-cni` entry untouched across §T.17) ∴ a REAL binary there survives data-dir rotation & retires the per-hop manual restart. do BEFORE §T.18 if it lands cheaply — else §V.50's restart is MANDATORY per worker. NB ⊥ an Istio VERSION change ∴ ⊥ blocked by §V.49's deferral|V50,V49
T47|.|`hop-verify.sh` ! gain a §V.50 check: ∀ node, `istio-cni-node` pod `startTime` > that node's k3s restart ∴ its `data/current` mount is fresh. DS-Ready ⊥ sufficient (§B.7: 3/3 Ready throughout the outage)|V50,V31
T48|x|DECIDED 2026-07-29: **option A — Istio ingress Gateway API** (§R.32). rationale = most production-ready of the 5; GatewayClass `istio` already Accepted; advances the Gateway API direction ∴ ⊥ a second migration later. CONSEQUENCE: §T.10 promoted deferred-follow-on → HARD PREREQUISITE (§V.59) & retargeted `1.29`→`1.30` (§V.60). options B/C/D/E MOOT ∴ the gloo-licence & traefik-history questions die with them|R.32,V55,V59,V60
T49|x|DONE 2026-07-30 — **18/20 migrated, 0 downtime, ∀ host matched its §T.50 baseline code**. 5 PR: `coroot` canary (#478) → batch1 ×6 (#479) → batch2 admin ×4 (#480) → `vault`+`dex` (#481) → final ×5 (#485). DNS-01 re-issue ≈ 90-180s each; old Secret serves until the new one is Ready ∴ ⊥ gap. `dex` OIDC discovery + `/keys` VERIFIED over the new cert ∴ Vault login path intact. REMAINING 2 ⊥ migratable: `langflow-ide` (§T.64, ⊥ ∈ repo) + `chores-tracker-agent-tls` (broken since May, §R.33). GOAL MET: cert-manager OFF the ingress critical path ∴ §V.52's silent ~30d renewal break ⊥ possible. 2 TRAPS HIT: (a) Argo app name ≠ directory name — `base-apps/kagent/`→app `kagent-secrets`, `base-apps/chores-tracker-backend/`→app `chores-tracker-backend` ⊥ `chores-tracker`; (b) §B.6 AGAIN — `chores-tracker-backend` reported `Synced` @ STALE rev `e2f92131` while main was `4f74c43b` ∴ the change silently ⊥ applied; hard refresh fixed it. ∴ §T.53 ! verify by OBSERVED REVISION, ⊥ by sync status|V52,R.27,R.28
T50|x|DONE 2026-07-29 → `docs/plans/ingress-migration-baseline-2026-07-29.md`. 19 host curled from allow-listed `73.7.190.154`; certs = 24 total / 23 Ready / 1 broken & issuer split 20 `letsencrypt-prod` + 1 `letsencrypt-route53` + 3 openshell — §R.27,§R.28,§R.33 CONFIRMED EXACTLY. deny leg proven from mobile LTE: `argocd`+`vault`+`dex` ALL 403 ∴ boundary enforced; `grafana` REACHABLE (login page) ∴ §R.31's "grafana has NONE" CONFIRMED live. deny spot-proven 3/14 — §V.53 mandates per-host proof @ CUTOVER, ⊥ @ baseline. NB `chores-agent` + `sandbox-1.vcluster` both answer `000`; `weather-kitchen` `/api/v1/health` = 404 ∴ ⊥ usable §V.62 probe, pick a real path|V53,R.33
T51|x|DONE 2026-07-31. RESEQUENCED from 'translate 17 rule in one pass @ the end' → INCREMENTAL: default-deny `AuthorizationPolicy` landed BEFORE the remaining host, each host ruled ALONGSIDE its route ∴ a host is never on the Gateway w/o a rule. 18 host ruled. **§V.61 PROVEN: request from LAN `10.0.1.182` (∉ list) → 403, & the access log records the REAL client addr ∴ `externalTrafficPolicy: Local` preserves it.** §V.66 HONOURED — `10.0.0.0/8` ⊥ carried over & costs NOTHING (router hairpin-NATs LAN → public addr). `atlantis` keeps its GitHub webhook range (IPv4+IPv6, 10 entry); public-by-design host carry ⊥ `from` clause|V53,V61,V66,R.31,R.34,R.35
T52|x|CLOSED 2026-08-01 by decision: the long-tail annotations are ⊥ needed & ⊥ carried over. GONE: `limit-rps`/`limit-connections` (7 Ingress) ∴ **⊥ rate limiting at the edge** — matters most on the public-by-design host (`n8n` webhook, `oncall-agent`, `grafana`) where IP allow-listing ⊥ available; `proxy-body-size` (3) — likely moot, Envoy streams & imposes ⊥ nginx's 1MB default; `proxy-read-timeout` 30-600s — Istio's effective default UNVERIFIED (⊥ readable from the config dump) ∴ if a shorter default applies, long request fail & look like an app fault. `auth-type: basic` ×1 dropped @ §T.57's sibling decision (kagent-mcp keeps the host + allow-list)|R.30,V57,V62
T53|x|DONE 2026-07-31 in 2 steps to keep the window short: (A) listeners → `:80`/`:443` while nginx still served, so klipper's `svclb` pods were scheduled & WAITING; (B) delete the HelmChart CR → nginx released the ports & klipper bound immediately. `coroot` polled **200 throughout** ∴ ⊥ observed downtime. deleting the CR (⊥ scaling down) was REQUIRED — helm-controller resurrects the DaemonSet unprompted (§V.22, observed). `hop-verify` `check_ingress` rewritten in the SAME change (§V.58) & now asserts 4 things: Gateway Programmed, data plane ready pods, ∀ listener resolved (§V.63), `AuthorizationPolicy` present (§V.61)|V54,V55,V58,V22,V46
T54|x|DECIDED 2026-07-30 by the human: **MAINTENANCE WINDOW ACCEPTABLE** (personal cluster) ∴ hard cutover permitted, ⊥ zero-downtime constraint. DERIVED SHAPE (⊥ the human's words — revisit @ §T.55): stage the Istio gateway on ALT PORTS (`:8080`/`:8443`) so it co-runs w/ nginx → validate ∀ host by `curl --resolve` → swap to `:80`/`:443` inside the window. rollback = revert git → nginx returns (§V.54 keeps old `Ingress` live throughout). CLIENT-IP mechanism still open @ §T.55: `hostNetwork: true` on `k3s-control-01` PREFERRED (exact nginx parity, real client IP FREE ∴ `ipBlocks` works w/o `externalTrafficPolicy` games) w/ `externalTrafficPolicy: Local` as fallback — §V.61's both-directions proof applies either way|V55,V61,R.35
T55|x|DONE 2026-07-31. `istio-ingress/main`, `gatewayClassName: istio`, 19 listener ∀ Programmed+ResolvedRefs, ⊥ conflict. **⊥ hostNetwork** — Istio permits low-port bind via the `net.ipv4.ip_unprivileged_port_start` sysctl, k8s FORBIDS that sysctl under hostNetwork, & Istio's image has ⊥ file capability to fall back on ∴ `NET_BIND_SERVICE` is INERT (`cannot bind '0.0.0.0:443': Permission denied`). nginx only manages it because ITS image carries `cap_net_bind_service`. ∴ klipper does the privileged bind & `externalTrafficPolicy: Local` carries the client addr. cert stay in app ns via `ReferenceGrant` (§V.63)|V63,V59,V60,R.26,R.34
T56|x|DONE 2026-07-31. 20 HTTPRoute / 18 hostname, ∀ in APP namespaces (backendRef same-ns ∴ ⊥ 2nd grant). §V.62 PROVEN by upstream-cluster names in the access log: `/api/docs` + `/api/openapi.json` → FRONTEND, `/api/v1/health` → BACKEND ∴ the negative lookahead reproduced w/o regex. the `rewrite-target: /api/$1` was an IDENTITY no-op & correctly DROPPED. `n8n` = 2 posture on 1 host, split by PATH in the authz policy. backend names read from LIVE Ingress (`coroot` → `coroot-coroot:8080`)|V62,V54,R.29,R.30
T58|~|grafana `11.3.1` → `11.3.9` (`base-apps/logging/grafana-deployment.yaml:83`). CVE-2025-3260 (high, authz bypass) + CVE-2025-2703 + CVE-2025-3454 fixed @ `11.3.5`; `11.3.6`-`11.3.9` + `-security-01` rebuilds followed ∴ `11.3.5` ⊥ the end of the line. same minor ∴ ⊥ config migration. EDIT MADE, ⊥ COMMITTED ∴ ⊥ in cluster yet. NB `11.3` line last shipped 2025-07-23 & grafana now @ `13.1.1` ∴ `11.3.9` = STOPGAP, ⊥ a supported line → §T.60|V67,V8
T59|.|grafana → **GitHub OAuth DIRECT** (⊥ via dex — DECIDED 2026-07-29). dex path REJECTED: dex `staticClients` ⊥ filter identities ∴ that path needs the SAME grafana-side filter AND `dex.arigsela.com` made publicly reachable (§V.68) = strictly more work + more exposure, ⊥ benefit. github.com is already public ∴ ⊥ mobile blocker. ORDER ! (secret BEFORE manifest — `envFrom` a missing Secret = grafana ⊥ start): (1) register GitHub OAuth App, callback `https://grafana.arigsela.com/login/github`; (2) client id+secret → Vault `k8s-secrets/grafana`, + ExternalSecret ∈ `logging`; (3) `GF_SERVER_ROOT_URL` `http://localhost:3000` → `https://grafana.arigsela.com` — OAuth `redirect_uri` derives from it ∴ localhost = broken flow; (4) `GF_AUTH_GITHUB_*` w/ BOTH fail-closed gate (§V.69): `ROLE_ATTRIBUTE_PATH` matching login `arigsela`, `ROLE_ATTRIBUTE_STRICT=true`, `ALLOW_SIGN_UP=false`; (5) pre-create the grafana user (PVC `grafana-storage` ∴ survives restart); (6) KEEP local admin as break-glass. ! prove w/ a 2nd GitHub identity|V69,V67,V8
T61|.|create a GitHub ORG & move `arigsela` into it → unlocks `allowed_organizations` (membership check, fails CLOSED) ∴ retires §V.69's fragile JMESPath, AND lets dex's connector set `orgs:` (`base-apps/dex/configmap.yaml:42-53`) which retires §V.68 & hardens **VAULT's** login too, ⊥ only grafana's. root fix for both paths. after §T.59|V68,V69
T60|.|grafana `11.3.x` → a SUPPORTED line. `11.3` last shipped 2025-07-23; current = `13.1.1` (2026-07-21) ∴ 2 major behind & the branch is EOL. §T.58 buys time, ⊥ a fix. NB major bumps carry dashboard/plugin/datasource migration ∴ own window, own rollback (§V.8). after §T.59|V67,V8
T62|x|DONE 2026-07-30. PROVED §V.69 gate (a) via route (i): `role_attribute_path` → `login == 'zzz-no-such-github-login'` (#476) → `arigsela` REFUSED, `[oauth.role_attribute_strict_violation] idP did not return a role attribute, but role_attribute_strict is set`, 0 `Successful Login`; reverted #477. ⊥ 2nd account needed & break-glass admin held throughout. KEY: a LINKED user bypasses signup ∴ gate (b) `false` ⊥ mask gate (a) — the earlier "closing forecloses the test" claim held only for the 2nd-account route. ∴ §V.69 fully satisfied; the `allow_sign_up` flip-flop needed to onboard a user is now SAFE|V69,R.31
T63|x|DONE 2026-07-30. ∀ 7 classified & the intent is now MACHINE-READABLE, ⊥ conversational: `ingress-policy` CI (`.github/workflows/validate.yaml`) accepts `whitelist-source-range` OR `arigsela.com/public-by-design: "<reason>"`, ⊥ a bypass (reason REQUIRED; an Ingress w/ neither still FAILS — both directions tested). VERDICT: **⊥ accidentally-exposed host ∃** — `n8n` webhook `webhook-receiver`; `oncall-agent` `slack-events-api` (Slack Events API inbound, `SLACK_SIGNING_SECRET` HMAC + `API_KEYS`, ⊥ `SLACK_APP_TOKEN` ∴ ⊥ Socket Mode ∴ inbound REQUIRED — restricting it would break Slack); `grafana` `github-oauth` (§V.69 2 gates); `chores` ×2 `public-app` (ASSUMED, ⊥ verified); `vault.local` `internal-hostname-not-routable`; `sandbox-1.vcluster` DELETED (#482+#484 — host was NXDOMAIN ∴ ⊥ ever reachable; cert renewed anyway since DNS-01 needs only TXT). §T.51 ! read these annotations, ⊥ blanket-apply (breaks webhook) ⊥ blanket-omit (exposes admin UI)|V53,R.31
T64|.|`langflow.arigsela.com` = ORPHANED INGRESS, ⊥ ∈ repo. live 131d w/ `letsencrypt-prod` + allow-list (3 IP) & Argo tracking-id `langflow-ide-config:...` naming an Application that ⊥ ∃ ∴ ⊥ prunable, ⊥ GitOps-editable, ⊥ migratable by §T.49 & WILL break @ §T.43 cutover (cert renewal + routing BOTH). same class as §T.40's `lg-agents` PVC. decide: adopt into `base-apps/langflow-ide/` \| delete \| document as out-of-band. ! before §T.53|V52,T49,R.29
T65|x|DONE 2026-07-30. `istio-gateway-api` `v1.2.0` → `v1.5.1` — the version Istio 1.30 REQUIRES, ⊥ latest `v1.6.1` (which only adds TCPRoute/UDPRoute GA). Istio 1.30 w/o v1.5.x makes **TLSRoute + ReferenceGrant INVISIBLE to istiod, silently** — & §V.63 makes ReferenceGrant load-bearing for the Gateway's cross-ns `certificateRefs`. VERIFIED ⊥ storage migration needed: ∀ stored version still served @ v1.5.1, incl. `referencegrants` `v1beta1` which REMAINS storage ∴ ⊥ the §R.3/§V.26 ESO trap. also ADOPTED `backendtlspolicies` (was bundle `v1.4.0`, managed by nothing). waypoints stayed Programmed throughout|V63,V26,R.37,R.3
T66|x|DECIDED 2026-07-30 = option (b): AMEND, ⊥ restructure. istiod stays ONE app; `targetRevision` bumps are the mechanism. §V.12 gains a bounded exception, §V.20 superseded for §T.10, §V.70 written as the compensating protocol (per-step gate + proven rollback on step 1). rationale: §R.23 says `istio-cni`+`ztunnel` ⊥ canary-able ANYWAY ∴ 6 rounds of app-shuffling would buy a safe control plane while the dataplane — the actual risk — still goes in-place per node. 2 of 45 ns enrolled. istiod down ∈ ambient = config PAUSE ⊥ outage. ∴ §T.10 unblocked on this axis; §T.65 remains|V12,V20,V70,R.23
T67|x|DONE 2026-07-31 (#508). `base-apps/istio-ingress/` gains `docs.md` + `runbook.md` + `catalog-info.yaml` + `mkdocs.yml`, re-added to `scripts/agent-docs-scope.txt` (21 app), `directory.exclude` added to the Argo Application (validator requires it), generated indexes refreshed. runbook captures what the migration cost to learn — hostNetwork ⊥ workable, the Gateway reading `Accepted=True` w/ ∀ listener green while ⊥ Deployment ∃, istiod ⊥ re-reconciling on ConfigMap change, 403-vs-404 triage, silently-certless listeners, & the `10.0.0.0/8` trap|V58,R.34,R.35
T68|.|**§V.5/§V.47 GATE NOW FAILS** — 16 app `Synced` but `health=Progressing`, ∀ of them apps that gained an `HTTPRoute` @ §T.56. cause: Argo has ⊥ health assessment for `gateway.networking.k8s.io/HTTPRoute` ∴ the resource reports health `None` & an app w/ unknown-health resource ⊥ ever reach `Healthy`. ⊥ drift — a MISSING HEALTH CHECK. NB `hop-verify` flags `sync != Synced` **OR** `health != Healthy` ∴ a plain `kubectl get app` (sync only) shows CLEAN & disagrees w/ the gate — that mismatch is itself worth knowing. fix = `resource.customizations.health.gateway.networking.k8s.io_HTTPRoute` Lua @ `argocd-cm` — BUT blocked by the SAME documented defect as `resource.exclusions`: the argocd Terraform module writes `server.config.*` (deprecated) while the chart reads `configs.cm.*` (`argocd.tf`, `templates/agent-docs/README.md`) ∴ global argo config ⊥ take effect. ! resolve before the next hop, else §V.5 blocks it|V5,V47,R.19
T57|.|`chores-tracker-backend` canary: decide keep-the-VirtualService \| install the Rollouts Gateway API plugin \| retire the canary DELIBERATELY (§V.65, §R.36). ⊥ leave dangling when `chores.arigsela.com/api/*` re-routes. NB whether the vsvc affects north-south TODAY = ? ∴ determine before deciding|V65,R.36

## §B BUGS
id|date|cause|fix
B1|2026-07-27|`k3s-backup.sh` online mode excluded `state.db` only. kine runs SQLite WAL ∴ live `-wal`/`-shm` shipped beside `.backup` snapshot → SQLite replays foreign WAL over restored db. found by drill, ⊥ by review|V15
B2|2026-07-27|`vault-backup.sh` cold mode scaled sts→0 to quiesce, but `replicas: 1` ∈ git (`base-apps/vault/statefulsets.yaml:18`) & Argo app `vault` `selfHeal: true` ∴ Argo rescales mid-copy → torn artifact BRANDED CONSISTENT. same class as §B.1. caught pre-exec by inspecting syncPolicy|V33
B3|2026-07-27|`vault-backup.sh` helper pod ⊥ toleration for `node-role.kubernetes.io/control-plane:NoSchedule` ∴ Pending forever — PV pinned to tainted control-plane & 2 workers ∉ PV affinity. backup aborted; §V.33 trap restored Argo + Vault safely ∴ ⊥ outage|V34
B4|2026-07-28|§V.33 suspend patched the CHILD Argo app only. `vault` app tracked by `master-app` & its `syncPolicy` ∈ git ∴ master-app re-synced @14:41:09Z MID-§T.36 & restored `automated` — suspension silently undone while the restore helper still held the volume. ⊥ harm (manual scale-up won the race) but `selfHeal` could have started Vault on half-restored data|V33
B5|2026-07-28|§T.37 patched `spec.affinity` on the live single-instance CNPG. §R.8 claimed affinity ⊥ rollout trigger — doc OMISSION read as absence, wrong: CNPG restarted the primary at once, its PVC pinned `k3s-control-01` ∴ Pending. compounded by `--type merge` MERGING the `nodeSelector` map → impossible `{hostname:worker-01, workload:infrastructure}`. ~5min outage `n8n`+`chores_tracker`, ⊥ data loss. NB a CORRECT patch fails identically — restarted primary ⊥ schedulable off its PV|V43,V44
B8|2026-07-29|DELAYED HALF OF §B.7, found 14h later while verifying §T.18. ∀ sandbox attempt during the §B.7 window got an IP from flannel BEFORE failing at the istio-cni step ∴ leaked the reservation. 18min of retries (3882 events) → 219/254 of `10.42.0.0/24` leaked → `k3s-control-01` ⊥ create ANY pod for 14h, SILENTLY: existing pods kept running & the only visible casualty was a `kube-system` CronJob pod ∉ any Argo app ∴ §V.5 was green throughout. found by chasing a lone `ContainerCreating` rather than dismissing it. NB my FIRST classifier said keep=0/stale=253 — `k3s` ⊥ ∈ PATH under `sudo sh -c` ∴ live-id list EMPTY ∴ would have deleted 14 LIVE reservations → IP collisions. caught by asking why 0 live pods matched. fix = prune 219 (KEEP on live-IP OR live-container-id), dir tarred first; canary pod on control-01 then ran to Completion|V51
B7|2026-07-28|§T.17 control-plane hop → k3s rotated `data/<hash>` & repointed `current`; the istio-cni binary stayed behind ∴ `data/cni/istio-cni` DANGLED → ∀ new pod sandbox on control-01 failed `failed to find plugin "istio-cni"` → coredns + local-path-provisioner + metrics-server ALL stuck ContainerCreating → CLUSTER DNS DOWN → argocd repo-server CrashLoop (its `/healthz?full=true` does a git remote lookup) + `helm-install-ingress-nginx` CrashLoop (`lookup kubernetes.github.io ... connection refused`) + ∀ 57 Argo app went sync=`Unknown`. the running istio-cni-node ⊥ self-heal — hostPath resolved at pod-create ∴ it reinstalled into the OLD dir. FIX = `kubectl delete pod` → fresh mount → binary in the new dir in ~10s, everything drained behind it. §V.9 = 27m27s vs 15m bound. ROOT MISS: §V.49 watched DS Ready ∀ node & all 3 were Ready THROUGHOUT ∴ the check was blind to the actual failure mode|V50
B10|2026-07-30|§V.69's `allow_sign_up=false` gate was UNREACHABLE from a cold start ∴ it denied the LEGITIMATE user on first login (`auth_module=oauth_github auth_id=2475907` = `arigsela`, "signup is not allowed"). cause: grafana 11 links an OAuth identity ONLY by `auth_module`+`auth_id`, created BY signup; `oauth_allow_insecure_email_lookup=false` by default ∴ §T.59 step 5 ("pre-create the grafana user") was IMPOSSIBLE as written. designed from the MECHANISM w/o checking the live default — same shape as §B.9. fix = bootstrap signup ON → link → close|V69
B9|2026-07-29|§V.61 modelled SNAT → "total lockout" from the Istio MECHANISM (`ipBlocks` = packet source IP) w/o reading the live allow-list CONTENTS. ∀ 15 list already carry `10.0.0.0/8` ⊇ node `10.0.1.0/24` + pod `10.42.0.0/16` ∴ SNAT makes the gateway's OWN address match → silent FULL-OPEN on `argocd`,`vault`,`dex`,`backstage`…, ∀ host 200, ⊥ symptom — the loud failure §V.61 promised NEVER ARRIVES. found while planning §T.50 by grepping the lists; §V.61's own review pass missed it because it reasoned from the mechanism, ⊥ from the data|V66
B6|2026-07-28|resumed Argo post-§T.37 w/o confirming observed revision. Argo synced CACHED `d6f8ddd` (pre-T37, selector=`workload:infrastructure`); client-side apply merged it w/ live `hostname` → impossible union AGAIN → Postgres down ~4min. then FLAPPED ~60s cycles from stale `last-applied-configuration` until repaired by `kubectl apply` of the git manifest. 2nd outage of the day, same shape as §B.5, different actor|V45

apiVersion: v1
kind: ServiceAccount
metadata:
  name: homelab-agent
  namespace: kagent
  labels:
    app.kubernetes.io/part-of: kagent
    app.kubernetes.io/name: homelab-agent
  # Pod identity for the BYO deployment (non-credential-bearing; the container
  # reads its secrets from env, not from Vault directly).

apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso-homelab-agent-db
  namespace: kagent
  # Same Vault k8s-auth role "homelab-agent-db" is bound to this SA in BOTH
  # the kagent and postgresql namespaces (role has both namespace bounds).

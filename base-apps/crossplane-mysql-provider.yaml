apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: crossplane-mysql-provider
  namespace: argo-cd
spec:
  project: default
  source:
    repoURL: https://github.com/arigsela/kubernetes
    targetRevision: fix/cni-recovery
    path: base-apps/crossplane-mysql-provider
  destination:
    server: https://kubernetes.default.svc
    namespace: crossplane-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

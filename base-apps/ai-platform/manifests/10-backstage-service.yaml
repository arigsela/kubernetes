---
# Source: ai-platform-engineering/charts/backstage-plugin-agent-forge/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ai-platform-backstage-forge
  labels:
    helm.sh/chart: backstage-plugin-agent-forge-0.1.0
    app.kubernetes.io/name: backstage-forge
    app.kubernetes.io/instance: ai-platform
    app.kubernetes.io/version: "0.1.0"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
    - port: 7007
      targetPort: backend
      protocol: TCP
      name: backend
  selector:
    app.kubernetes.io/name: backstage-forge
    app.kubernetes.io/instance: ai-platform

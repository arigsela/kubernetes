apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: langflow-runtime-config
  namespace: argo-cd
spec:
  project: default
  source:
    repoURL: https://github.com/arigsela/kubernetes
    targetRevision: main
    path: base-apps/langflow-runtime
  destination:
    server: https://kubernetes.default.svc
    namespace: langflow-runtime
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

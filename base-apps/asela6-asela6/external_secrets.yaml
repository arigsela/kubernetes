apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: asela6-secret
  namespace: asela6
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: asela6-secret
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: asela6
        property: DB_PASSWORD
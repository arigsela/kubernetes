apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agent-ui-frontend
  namespace: argo-cd
spec:
  project: default
  source:
    repoURL: https://github.com/arigsela/kubernetes
    targetRevision: main
    path: base-apps/agent-ui-frontend
  destination:
    server: https://kubernetes.default.svc
    namespace: agent-ui-frontend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

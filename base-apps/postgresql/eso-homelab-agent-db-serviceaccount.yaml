apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso-homelab-agent-db
  namespace: postgresql
  # Vault binds the kubernetes-auth role "homelab-agent-db" to THIS ServiceAccount;
  # that role's policy reads only k8s-secrets/data/homelab-agent-db.

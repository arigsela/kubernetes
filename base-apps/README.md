# Maintenance Mode

This branch is in maintenance mode with most applications temporarily removed.
This allows the cluster to start with minimal load.

**Retained Applications:**
- MySQL (database - preserves data)
- PostgreSQL (database - preserves data)

To restore normal operations, switch back to the `main` branch.

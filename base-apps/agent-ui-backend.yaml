apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agent-ui-backend
  namespace: argo-cd
spec:
  project: default
  source:
    repoURL: https://github.com/arigsela/kubernetes
    targetRevision: main
    path: base-apps/agent-ui-backend
  destination:
    server: https://kubernetes.default.svc
    namespace: agent-ui-backend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

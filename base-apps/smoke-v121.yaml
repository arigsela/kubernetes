# ArgoCD Application for smoke-v121
# Sync wave 10 ensures XRD/Composition land before this app's XR.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: smoke-v121
  namespace: argo-cd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: default
  source:
    repoURL: https://github.com/arigsela/kubernetes
    targetRevision: main
    path: base-apps/smoke-v121
  destination:
    server: https://kubernetes.default.svc
    namespace: smoke-v121
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

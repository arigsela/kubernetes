apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: asela5-secret
  namespace: asela5
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: asela5-secret
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: asela5
        property: DB_PASSWORD
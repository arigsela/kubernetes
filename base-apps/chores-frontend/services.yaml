apiVersion: v1
kind: Service
metadata:
  name: chores-frontend
  namespace: chores-frontend
  labels:
    app: chores-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: chores-frontend
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: test-mysql-1-secret
  namespace: test-mysql-1
  labels:
    app.kubernetes.io/name: test-mysql-1
    app.kubernetes.io/instance: test-mysql-1-mysql
    app.kubernetes.io/component: database-secret
    app.kubernetes.io/part-of: test-mysql-1
    environment: development
    backstage.io/kubernetes-id: test-mysql-1
  annotations:
    backstage.io/kubernetes-id: test-mysql-1
    backstage.io/kubernetes-namespace: test-mysql-1
spec:
  refreshInterval: 30s
  secretStoreRef:
    name: secret-store
    kind: SecretStore
  target:
    name: test-mysql-1-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        # Database password from Vault
        DB_PASSWORD: "{{ .password | toString }}"
  data:
    - secretKey: password
      remoteRef:
        key: test-mysql-1/DB_PASSWORD
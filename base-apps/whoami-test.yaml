apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: whoami-test
  namespace: argo-cd
spec:
  project: default
  source:
    repoURL: https://github.com/arigsela/kubernetes
    targetRevision: replce-traefik-with-nginx
    path: base-apps/whoami-test
  destination:
    server: https://kubernetes.default.svc
    namespace: whoami-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
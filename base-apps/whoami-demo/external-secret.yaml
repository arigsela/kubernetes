# Seed the Vault key k8s-secrets/whoami-demo with your app's secrets;
# every property under it is synced into the whoami-demo-secrets Secret.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: whoami-demo-secrets
  namespace: whoami-demo
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: whoami-demo-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: whoami-demo

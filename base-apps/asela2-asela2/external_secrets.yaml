apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: asela2-secret
  namespace: asela2
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: asela2-secret
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: asela2
        property: DB_PASSWORD
# Service for OnCall Agent API
apiVersion: v1
kind: Service
metadata:
  name: oncall-agent
  namespace: oncall-agent
  labels:
    app: oncall-agent-api
spec:
  type: ClusterIP
  selector:
    app: oncall-agent-api
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
# IAM Policy for OnCall Agent via Crossplane
# Grants read-only access to CloudWatch metrics and EC2 NAT Gateway information
# Follows AWS best practices for service account permissions

apiVersion: iam.aws.upbound.io/v1beta1
kind: Policy
metadata:
  name: oncall-agent-nat-monitor-policy
  labels:
    app: oncall-agent
    component: iam-policy
    managed-by: crossplane
spec:
  forProvider:
    description: "Read-only access to CloudWatch NAT Gateway metrics and EC2 NAT Gateway information for OnCall Agent"
    policy: |
      {
        "Version": "2012-10-17",
        "Statement": [
          {
            "Sid": "CloudWatchNATMetricsReadOnly",
            "Effect": "Allow",
            "Action": [
              "cloudwatch:GetMetricStatistics",
              "cloudwatch:ListMetrics"
            ],
            "Resource": "*"
          },
          {
            "Sid": "EC2NATGatewayReadOnly",
            "Effect": "Allow",
            "Action": [
              "ec2:DescribeNatGateways",
              "ec2:DescribeVpcs",
              "ec2:DescribeSubnets"
            ],
            "Resource": "*"
          }
        ]
      }
    tags:
      Name: "OnCall Agent NAT Monitor Policy"
      Purpose: "NAT-Gateway-Monitoring"
      ManagedBy: "Crossplane"
      Environment: "homelab"
      Project: "asela-chores"
  providerConfigRef:
    name: default

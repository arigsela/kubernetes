apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: oncall-agent
  namespace: oncall-agent
  annotations:
    agent-docs/path: docs.md
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-label-selector: 'app=oncall-agent-api'
    backstage.io/kubernetes-namespace: oncall-agent
  tags: [ai-agent, anthropic, incident-response, slack]
spec:
  type: service
  lifecycle: production
  owner: group:default/platform
  system: default/platform-observability
  dependsOn:
    - resource:vault/vault

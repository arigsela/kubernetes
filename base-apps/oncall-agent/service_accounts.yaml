# ServiceAccount for OnCall Agent API
# Needs read access to K8s resources for monitoring
apiVersion: v1
kind: ServiceAccount
metadata:
  name: oncall-agent-api-sa
  namespace: oncall-agent
  labels:
    app: oncall-agent-api
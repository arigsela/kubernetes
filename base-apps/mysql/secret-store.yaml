apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: vault-backend
  namespace: mysql
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      path: "k8s-secrets"  # Your KV engine
      version: "v2"        # KV v2 engine
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "mysql"
          serviceAccountRef:
            name: "default"
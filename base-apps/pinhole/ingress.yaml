apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: pihole
  namespace: dns
spec:
  entryPoints:
    - web
  routes:
    - match: Host('pihole.arigsela.com')
      kind: Rule
      services:
        - name: pihole-tcp
          port: 80
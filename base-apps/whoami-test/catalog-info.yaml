apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: whoami-test
  namespace: whoami-test
  annotations:
    agent-docs/path: docs.md
    backstage.io/techdocs-ref: dir:.
    backstage.io/kubernetes-label-selector: 'app=whoami-test'
    backstage.io/kubernetes-namespace: whoami-test
  tags: ["test", "whoami"]
spec:
  type: service
  lifecycle: experimental
  owner: group:default/platform
  system: system:default/platform-tooling
  dependsOn:
    - resource:vault/vault

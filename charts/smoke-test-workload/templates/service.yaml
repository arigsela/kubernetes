apiVersion: v1
kind: Service
metadata:
  name: smoke-app
  labels:
    app.kubernetes.io/name: smoke-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.port }}
  selector:
    app.kubernetes.io/name: smoke-app
